# NeuroInfoGrinder Project Memory

Last updated: 2026-07-02

Current status:
- DEPLOYED SHADOW GATE + CLEANED UI + SAVED DATASET (Adam: "исследуй еще раз, допили и деплой, удали ВСЕ лишние материалы, UI чисто, сообщения сохрани как датасет на будущее"): (1) Researched the 37 Java-gate eligible on 20k by hand, found one clear bug — `[NIGTEST-A02]` test artifact leaked as eligible -> added `TEST_ARTIFACT` regex (NIGTEST/NIGTOP/ZAUR prefixes) to BOTH Python lab and Java gate -> REJECT_SAFE; added gold regression (Python 37/37+15/15, Java gate 23/23, full `mvn test` 169/169). The 11 model-news (Claude Sonnet 5 release, GPT-5.6 METR, OpenHuman, ClinePass, PlantTalk, etc.) are correctly left for the production `MessageUsefulnessClassifier` to map UNVERIFIED_MODEL_CLAIM -> SIGNAL_ONLY (the gate is pre-classifier; duplicating that in the gate would be over-engineering). 5 Caveman SKILLS promo reposts -> dedup handles in production. (2) Saved future dataset: `reports/material-selection-lab-v2/dataset/material_dataset_v1.jsonl` — 39 hand-labeled rows (22 keep = real technical guides/references + r-api cluster; 11 review = model-news/borderline; 6 reject = promo dupes/test artifact) with raw_id, text, sender, strong_entities, label, category, note. Full 20k snapshot stays at `data/raw_messages_20k_rich.jsonl`. (3) Deployed backend-only with the gate via `scripts/deploy-fast.ps1 -Target backend` (the script's ssh-pipe aborted on docker build stderr noise, so finished the build+up manually via a remote script; only `backend` recreated, postgres/redis/tor/ssh-socks/model-worker untouched, backend healthy, public URL 200). (4) Enabled both shadow settings via `POST /api/v2/pipeline/settings/{key}`: `decisionCoreShadowEnabled=1` + `materialEligibilityGateShadowEnabled=1` — gate now logs v2 verdicts to `semantic_decision_objects` on every normalization; still shadow only (no materialization/routing change). (5) Deleted ALL 164 DRAFT materials (ids 127-141, 211-360) via public `DELETE /api/v1/materials/{id}` — all from confirmed-bad generation runs, 0 published. Verified `materials_remaining=0` across all 10 topics -> UI materials page clean. Signals (441 across topics) intentionally left: no public signal delete endpoint and they are the by-design rejected-signal stream, not "материалы"; Adam asked to clean materials. NOT done: gate is shadow-only (logs verdicts, does not block materialization); going active (blocking candidates the gate marks INELIGIBLE before LLM Judge) is the next step after shadow validates against real runs. No reprocess/backfill/generation/TG-session/provider changes.
- MATERIAL LAB v2 NOW HAS AUTOMATED GOLD VERIFICATION (GREEN) + IMPROVED RULES: Built a grounded gold test dataset and an automated verify harness so the pipeline is checked deterministically instead of by eye. Added `reports/material-selection-lab-v2/gold/message_test_cases.jsonl` (25 cases covering rules/moderation/risk/abuse/promo-airdrop/job/event/link-only/short-chatter/unverified-claim/technical/outage/howto/tool-resource/hidden-referral/weak-signal) and `cluster_test_cases.jsonl` (6 cases covering multi-source material, single-source signal group, promo-blocked, rules-not-material, weak-pair-not-material, link-enrichment). Added `scripts/verify_material_lab_v2.py` that runs each case through prepare->normalize->route and cluster decision logic and reports pass/fail. First run was 21/25 messages and 2/6 clusters; fixed five real bugs found by the dataset: (1) tech uplift so troubleshooting with a strong entity (codex/droid problem reports) routes to `REVIEW_HIGH_RECALL` instead of `SIGNAL_ONLY`; (2) unverified-claim detection now requires claim indicators (бесплатн/доступен/free/claim/релиз/цена) not just a model name, so technical mentions of codex/opus are not downgraded; (3) hidden referral URL detection from `raw_json` entities (`ref=`/`start=ref`) routes to `MANUAL_REVIEW`; (4) tool/resource with link + context uplift to `REVIEW_HIGH_RECALL`; (5) cleaned `cluster_decision` so `MANUAL_REVIEW`/`LINK_ENRICHMENT_FIRST` take precedence over all-risk BLOCKED and `NEEDS_ENRICHMENT` is no longer treated as risk. Also added `google.com`/`play.google.com`/`docs.google.com` to `GENERIC_MERGE_DOMAINS` after the dataset surfaced a false material candidate merging an app link and a form by the generic `google.com` domain. Final verify: `25/25` messages pass, `6/6` clusters pass. Final 20k rerun with improved rules: routes `CONTEXT_ONLY=18806`, `REJECT_SAFE=515`, `REVIEW_HIGH_RECALL=197` (up from 94, droid/codex now caught), `SIGNAL_ONLY=149`, `NEEDS_ENRICHMENT=140`, `MANUAL_REVIEW=99`, `AGGREGATE_ONLY=94`; clusters `494`, decisions `REVIEW_SINGLE_SIGNAL=212`, `LINK_ENRICHMENT_FIRST=117`, `MANUAL_REVIEW=72`, `CONTEXT_ONLY=49`, `SIGNAL_PAIR=16`, `CONTEXT_GROUP=14`, `WEAK_PAIR=7`, `SIGNAL_GROUP=6`, `EVIDENCE_GROUP_REVIEW=1` (only `r-api` technical discussion, 3 msgs, 2 independent senders). No production writes/deploy/generation/reprocess; offline-only.
- MATERIAL LAB v2 LATEST HARDENING: Added `raw_json` entity URL extraction to `scripts/material_lab_v2.py`, so hidden/caption `textEntityTypeTextUrl` links are included in `has_link`, `domains`, `strong_entities`, routing, clustering, and evidence. Rich snapshot contains 1,290 hidden `textEntityTypeTextUrl` entities and 407 URL entities; v2 now extracts 1,208 hidden URLs across 722 messages. Added promo/risk hardening for free-token/airdrop/presale/Senpi-style campaigns and tightened `EVIDENCE_GROUP_REVIEW` so pure `SIGNAL_ONLY` groups cannot become material candidates without at least one `REVIEW_HIGH_RECALL` member. Latest full local v2 run (offline only, no production writes/deploy/generation): routes `CONTEXT_ONLY=18817`, `REJECT_SAFE=515`, `SIGNAL_ONLY=276`, `NEEDS_ENRICHMENT=126`, `REVIEW_HIGH_RECALL=94`, `AGGREGATE_ONLY=94`, `MANUAL_REVIEW=78`; clusters `496` total with decisions `REVIEW_SINGLE_SIGNAL=197`, `BLOCKED=188`, `CONTEXT_ONLY=49`, `SIGNAL_PAIR=18`, `CONTEXT_GROUP=13`, `SIGNAL_GROUP=9`, `MANUAL_REVIEW=7`, `LINK_ENRICHMENT_FIRST=7`, `WEAK_PAIR=7`, `EVIDENCE_GROUP_REVIEW=1`. Only current material-level candidate is `r-api` technical discussion (`3` messages, `2` independent senders, `1` conversation). `droid`/`codex`/`opus`/`deepseek` moved to signal groups pending manual review instead of automatic material candidates.
- MATERIAL SELECTION LAB v2 (CONVERSATION-FIRST + STRONG-ENTITY MERGE) IMPLEMENTED AND RUN ON RICH 20K SNAPSHOT: Audited full production pipeline (ReplayV2Service stages, MessageUsefulnessClassifier, classical-ML inert blocks, weak cluster admission, single-source over-materialization, lightweightOverride bypassing LLM Judge reject). Found root offline cause: v1 20k snapshot lacked reply_to_message_id/sender_id/sender_name/raw_json, so reply-chain context and independent-source checks were broken. Created richer read-only SELECT `reports/material-selection-lab-v2/export_raw_messages_20k_rich.sql` and pulled 20,000 rows from production DB `neuroinfogrinder2_prod_clean` (NOT `neuroinfogrinder`) via SSH inside postgres container, server-side UTF-8 file + scp; cleaned server temp. Rich coverage: 20,000 rows, 8,046 with reply_to, 20,000 with sender_id, 1,148 unique senders, 79 chats. Built `scripts/material_lab_v2.py` with two-phase clustering: Phase 1 conversation-local clusters of CANDIDATE messages only (union by reply-chain OR strong-entity overlap; time window is context only, NOT a merge condition; non-candidate nearby messages attached as context for display but do not drive decision); Phase 2 cross-conversation merge ONLY by specific merge-eligible entity (non-generic domains/specific APIs/error-codes), excluding generic model/tool names (opus/claude/codex/droid/gemini) and generic platforms (github.com/youtu.be/t.me) that caused giant transitive components; size cap 20. Decisions: size-2 -> WEAK_PAIR/SIGNAL_PAIR not material; EVIDENCE_GROUP_REVIEW requires size>=3 and independent_source_count>=2 (by sender_id); error codes strong only with error context; rules/onboarding majority -> BLOCKED. Final v2 run: routes REJECT_SAFE=515, MANUAL_REVIEW=21, NEEDS_ENRICHMENT=128, AGGREGATE_ONLY=96, SIGNAL_ONLY=303, REVIEW_HIGH_RECALL=92, CONTEXT_ONLY=18845; 507 clusters with decisions REVIEW_SINGLE_SIGNAL=218, BLOCKED=168, CONTEXT_ONLY=71, SIGNAL_PAIR=17, CONTEXT_GROUP=11, WEAK_PAIR=8, SIGNAL_GROUP=4, EVIDENCE_GROUP_REVIEW=4, LINK_ENRICHMENT_FIRST=4, MANUAL_REVIEW=2. Material candidates now topically coherent: `droid` tool issues (5 msgs, 4 indep sources), `r-api` balancer discussion (3 msgs, 2 indep), `codex` reconnect issues (3 msgs, 2 indep), plus `opus`/`deepseek`/`claudecode` signal groups. Fixed three bugs iteratively: rules/onboarding cluster falsely SIGNAL_GROUP via shared domain -> BLOCKED; bare `400`/`73.400` false positive as error-code strong -> requires error context; giant 253-msg cluster from transitive cross-conversation merge via generic `opus`/`codex` -> merge-eligibility excludes generic model names + size cap. Outputs in `reports/material-selection-lab-v2/`: routes, clusters (`russian_cluster_summary.md/.csv` with Russian names by shared entity + conversation refs + top-20 messages with class/route/sender/time/reply + context samples), evidence groups, gold template. No production writes/deploy/generation/reprocess; read-only SELECT only.
- RUSSIAN CLUSTER NAMING ADDED, CLEANUP CANDIDATES REPORTED ONLY: Added offline helper `scripts/name_material_lab_clusters.py` to read lab `clusters_*.jsonl` and produce Russian cluster names plus top-20 messages/classes per cluster. Verified `python -m py_compile scripts/name_material_lab_clusters.py`, then ran `python scripts/name_material_lab_clusters.py --only-review --top-per-algorithm 30 --top-messages 20`. Outputs: `reports/material-selection-lab/out/cluster_review/russian_cluster_summary.md` and `.csv`, naming 150 review clusters: `api_tools=91`, `mixed=30`, `outage_status=27`, `model_provider_claims=1`, `jobs_career=1`. Also created `reports/material-selection-lab/out/cleanup_candidates.md` with keep/remove/archive candidates. No files were deleted or archived; cleanup requires explicit Adam approval.
- OFFLINE MATERIAL SELECTION LAB IMPLEMENTED AND RUN ON 20K SNAPSHOT: Added local-only CLI `scripts/material_selection_lab.py` with commands `prepare`, `normalize`, `route`, `context`, `cluster`, `cluster-review`, `markers`, `evidence`, `gold-template`, `evaluate`, and `all`. The script writes only under `reports/material-selection-lab/` and `.docs/research/pipeline/material-quality/lab/`, preserves `raw_text`/`raw_json`, keeps `raw_id`/`dataset_message_id` traceability, emits `route_reason` for every routed row, and does not call production APIs/DB/model-worker/providers/generation or deploy anything. Ran `python scripts/material_selection_lab.py all --max-pairwise 300` on `reports/raw-messages-20k-20260630.jsonl`: prepared/normalized/routed 20,000 messages; route counts `CONTEXT_ONLY=18975`, `REJECT_SAFE=410`, `SIGNAL_ONLY=254`, `NEEDS_ENRICHMENT=123`, `REVIEW_HIGH_RECALL=111`, `AGGREGATE_ONLY=109`, `MANUAL_REVIEW=18`; built 615 context windows, compared 12 algorithm slots (5 dependency-free baselines ran, optional BGE/HDBSCAN/KMeans/Agglomerative/BERTopic/supervised adapters documented but not run), created 273 marker groups, 713 evidence groups, 811-row gold-label template, eval/final draft policy artifacts. Validation: `python -m py_compile scripts/material_selection_lab.py` passed. No production writes, deletes, deploy, generation, reprocess, provider/model-worker calls, or Telegram/session/proxy changes were performed.
- 20K FULL PIPELINE GENERATION RUN COMPLETED, QUALITY REGRESSION CONFIRMED: Adam explicitly approved running production generation over all 20k messages with effectively unlimited budget. Created parent dataset `15764` from latest 20,000 non-empty `raw_messages`. Direct V2 run with `maxMessages=20000` was backend-clamped to `5000` and failed on model-worker timeout, rolling back with no committed run/materials. 1,000-message chunks also hit BGE/model-worker timeout, so final run used 100 mini datasets of 200 messages each (`15814-15913`) and completed `100/100` full V2 replay runs. Results: `20,000` messages processed, `465` provider calls, estimated cost `$5.148730`, `116` DRAFT materials generated (`REFERENCE=72`, `ANSWER=23`, `SUMMARY=13`, `GUIDE=5`, `GENERATION=3`; `SINGLE_MESSAGE=77`, `MACRO=39`). Evidence: `reports/full-pipeline-20k-generation-run-20260701.md`, `reports/full-pipeline-20k-mini-replays-20260630.tsv`, `reports/full-pipeline-20k-summary-20260630.json`, `reports/full-pipeline-20k-materials-20260630.jsonl`. Post-run daily audit `reports/daily-materials-signals-audit-20260701T035836Z.md/.json/.html` shows severe over-materialization: `124` materials in window, `115` flagged; `241` signals, `167` flagged. Issue groups include `P0_RULES_ONBOARDING_DELETE=18`, `P1_RISK_MANUAL_REVIEW=20`, `P1_UNVERIFIED_MODEL_CLAIM=4`, `P2_JOB_POST_NOT_MATERIAL=5`, `P2_EVENT_NOT_MATERIAL=3`, plus many single-source materials. No cleanup/delete was performed yet; explicit approval needed before deleting generated materials/signals.
- 20K EVIDENCE BUNDLE CREATED: Added dedicated folder `reports/full-pipeline-20k-generation-20260701/` containing valid `materials.valid.json` array with all 116 generated DRAFT materials, `run-summary.valid.json`, `mini-runs.tsv`, copied post-run audit JSON/Markdown, `run-report.md`, SQL/export scripts, `README.md`, and `agent-pipeline-research-brief.md`. Validated `materials.valid.json` parses as an array with `116` objects, first material id `218`, last `334`; summary shows `20,000` messages and `100` runs. This is evidence only, not accepted production knowledge.
- BACKEND GUARDS DEPLOYED + BAD DAILY MATERIALS DELETED: Adam asked what to do, to fix it, and to delete the materials. Root cause: final materialization trusted single-message candidates/LLM fallback too much and the classifier did not block ChatKeeper/rules, moderation bot, safety-refusal/meta replies, unverified model/pricing claims, and job/event announcements before generation. Verified local targeted `MessageUsefulnessClassifierTest` and full backend `mvn -q test` passed. Deployed backend-only via `scripts/deploy-fast.ps1 -Target backend`; only `backend` was recreated, status check shows `backend` healthy, `frontend` up 22h, `model-worker` up 2d, `postgres/redis/tor` up 11d, `ssh-socks` up 10d, public URL 200. Soft-deleted all 57 active flagged materials from the daily window via public `DELETE /api/v1/materials/{id}`: ids `154-210`. Post-cleanup audit `reports/daily-materials-signals-audit-20260630T201505Z.md/.json/.html` shows `Materials in window: 0, flagged: 0`; signals remain `45`, flagged `34` because no public signal delete endpoint exists and no direct DB signal cleanup was performed. No reprocess/backfill/provider/model-worker calls, no prompt/settings/TG session changes.
- DAILY MATERIAL QUALITY REGRESSION AUDITED + LAB/CLASSIFIER IMPROVED: Adam asked to improve clustering/classification further and inspect today's generated materials. Ran read-only daily audit for `2026-06-30 03:00 MSK`; latest report `reports/daily-materials-signals-audit-20260630T195053Z.md/.json/.html` shows 57 materials in window and 57 flagged; 43 signals, 32 flagged. Material issue groups: `P0_RULES_ONBOARDING_DELETE=24`, `P0_REJECT_SHAPED_DELETE=4`, `P1_UNVERIFIED_MODEL_CLAIM=7`, `P1_RISK_MANUAL_REVIEW=2`, `P2_POSSIBLY_USEFUL_BUT_SINGLE_SOURCE=10`, `P2_WEAK_OTHER_SINGLE_SOURCE=6`, `P2_EVENT_NOT_MATERIAL=3`, `P2_JOB_POST_NOT_MATERIAL=1`. Added source/content previews to daily audit reports. Root causes visible in source samples: ChatKeeperBot/Russian IT rules became materials, LolsBot moderation became material, safety-refusal/meta replies became materials, unverified model/pricing rumor posts from bots became materials, and job/event posts were materialized from one source. Improved offline lab and backend classifier locally, not deployed: `scripts/message_lab_core.py`, `scripts/compare_message_lab_algorithms.py`, `scripts/cluster_message_lab.py`, `scripts/audit-daily-materials-signals.js`, and backend `MessageUsefulnessClassifier` tests/guards. Main lab default is now `pool_graph`; new 20k output `reports/message-lab-20k-20260630-v2.md/.json`: 20,000 messages, 19,681 clusters, 138 review clusters; classes now include `CAREER_JOB_POST=95`, `MODEL_RUMOR_OR_PRICING_CLAIM=69`, `EVENT_ANNOUNCEMENT=14`, `MODERATION_BOT_EVENT=75`, reducing false material candidates. Backend targeted classifier tests and full `mvn -q test` passed. No deploy, cleanup, production write, reprocess/backfill, provider/model-worker call, or prompt/settings change.
- OFFLINE MESSAGE LAB CLASSIFICATION/CLUSTERING IMPROVED: Adam asked to improve classification/clustering and compare algorithms before choosing. Extended `scripts/message_lab_core.py` with more specific classes and semantic pools: `MODERATION_BOT_EVENT`, `SOCIAL_MEDIA_LINK`, `INTERNAL_TELEGRAM_LINK`, `RESOURCE_LINK`, `REFERRAL_OR_INVITE_LINK`, `OUTAGE_STATUS`, `DIGEST_NEWS`, plus stricter abuse/risk handling for Discord/free-access/boost/ref patterns. Added offline comparison runner `scripts/compare_message_lab_algorithms.py` and sweep runner `scripts/sweep_message_lab_temporal.py`; both are local-only and do not call production DB/API/provider/model-worker. Final key report: `reports/message-lab-algorithm-comparison-20k-20260630-final.md/.json`; parameter sweep: `reports/message-lab-temporal-sweep-20k-20260630.md/.json`. On 20k export, final class counts: `LOW_VALUE_CHATTER=19047`, `RULES_ONBOARDING=335`, `WEAK_SIGNAL=217`, `TECH_SIGNAL=129`, `MODERATION_BOT_EVENT=75`, `SOCIAL_MEDIA_LINK=42`, `LINK_SHARE=39`, `INTERNAL_TELEGRAM_LINK=36`, `OUTAGE_STATUS=26`, `DIGEST_NEWS=15`, `RISK_PROMO_REFERRAL=11`, `REFERRAL_OR_INVITE_LINK=10`, `RESOURCE_LINK=8`, `ABUSE_OR_CIRCUMVENTION=7`, `POTENTIAL_DISCUSSION_SIGNAL=3`. Compared `temporal_pool_graph`, `pool_graph`, `token_dbscan`, earlier also `baseline_graph` and `canopy_signatures`. Recommendation for next calibration: `temporal_pool_graph` with threshold `0.25`; it produced 145 review clusters, 42 multi-message review clusters, 103 singleton review clusters, usefulGroupingRate `0.494`, reviewPurityRate `1.0`, no blocked leaks, no oversized clusters. `pool_graph` is close but mixes more cross-topic link queues; `token_dbscan` over-merged broad API clusters. No production writes/reprocess/backfill/deploy/provider/model-worker calls.
- 20K OFFLINE MESSAGE LAB RUN COMPLETE: Adam asked to run the lab over 20k messages. Public API was capped at 200, so performed read-only production DB export from `raw_messages` using `SELECT` only. Production has `24089` raw rows. Exported latest 20,000 non-empty raw messages to UTF-8 JSONL via server-side file to avoid Windows mojibake: `reports/raw-messages-20k-20260630.jsonl`; helper SQL: `reports/export-raw-messages-20k-20260630.sql`. Ran `scripts/cluster_message_lab.py`, outputs: `reports/message-lab-20k-20260630.md/.json`. Results: 20,000 messages, 16,564 lab clusters, 165 review clusters. Class counts: `LOW_VALUE_CHATTER=19141`, `RULES_ONBOARDING=335`, `WEAK_SIGNAL=222`, `TECH_SIGNAL=168`, `LINK_SHARE=121`, `RISK_PROMO_REFERRAL=11`, `ABUSE_OR_CIRCUMVENTION=2`. Top useful cluster `lab-cluster-7601` has 25 `TECH_SIGNAL` messages about r-api/API/VPN/connectivity in topic `Баги и вопросы по API`; top link queues include Telegram referral links, YouTube bundles, GitHub/resource links. No production writes/reprocess/backfill/provider/model-worker/deploy.
- OFFLINE MESSAGE LAB SCRIPTS ADDED, NOT IN APP: Adam proposed calibrating classification/clustering manually before productizing it. Added dependency-free Python lab scripts: `scripts/message_lab_core.py` (normalization, feature extraction, multi-axis classifier, graph clustering), `scripts/classify_live_messages.py` (one message or small batch), and `scripts/cluster_message_lab.py` (batch classify+cluster JSON/JSONL/CSV/TXT and write JSON/Markdown). No production DB/API/provider/model-worker writes. Smoke ran on `reports/daily-materials-signals-audit-20260630T090844Z.json`, producing `reports/message-lab-clusters-20260630T090844Z.md/.json`: 200 messages, 59 lab clusters, 2 review clusters; class counts `LOW_VALUE_CHATTER=184`, `MEDIA_ONLY_OR_EMPTY=9`, `WEAK_SIGNAL=3`, `RULES_ONBOARDING=2`, `LINK_SHARE=1`, `TECH_SIGNAL=1`. Best candidate remains raw `24787` as `TECH_SIGNAL`; raw `24831` is `LINK_SHARE/NEEDS_LINK_ENRICHMENT`. Python compile passed. Note: PowerShell inline Cyrillic args can mojibake; prefer UTF-8 files/JSONL for live batches.
- DAILY BAD MATERIAL CLEANUP EXTENDED: Adam asked to delete all bad items and identify useful clusters/classes. Soft-deleted active flagged materials `145`, `148`, `151`, `152`, and `153` via public `DELETE /api/v1/materials/{id}`. Re-ran daily audit: `reports/daily-materials-signals-audit-20260630T090844Z.html/.md/.json`; active materials in the `2026-06-30 03:00 MSK` window are now `0`, flagged materials `0`. Signals remain `15` with `11` flagged because no public signal delete endpoint exists and no direct production DB write was performed. Useful/review clusters in latest raw window are `LINK_SHARE` cluster `daily-cluster-21` raw `24831` needing link enrichment, and `TECH_SIGNAL` clusters `daily-cluster-25` raw `24787` (Yandex postamat MVP interview task) plus `daily-cluster-23` raw `24792` (weak Claude Code chat signal). No reprocess/backfill/provider/prompt/TG session/deploy changes.
- DAILY REPORT NOW SHOWS CLUSTER MESSAGE CONTENTS: Adam asked to show which messages are inside daily clusters and how the report will look. Updated `scripts/audit-daily-materials-signals.js` so `rawClusters` include per-message samples (`id`, MSK time, chat/topic, content type, class, readiness, risk, preview). Markdown now has `Cluster Message Samples`; HTML now has portal-style expandable `<details>` blocks per cluster, auto-open for `REVIEW_SIGNAL_OR_DISCUSSION` clusters. Regenerated latest report: `reports/daily-materials-signals-audit-20260630T085839Z.html/.md/.json` and opened the HTML locally. Latest live window changed since previous cleanup: 5 materials, all flagged; 15 signals, 11 flagged. No production writes/deploy/reprocess/backfill/provider calls.
- DAILY REPORT PORTAL OUTPUT AND SCOPED MATERIAL CLEANUP COMPLETED: Adam approved updating old bad materials and asked for portal-style reports plus daily classification/clustering. Extended `scripts/audit-daily-materials-signals.js` to produce HTML + Markdown + JSON, collect raw messages from `/api/v2/pipeline/live/stages/telegram_ingest/messages`, classify raw daily messages deterministically (`LOW_VALUE_CHATTER`, `MEDIA_ONLY_OR_WEAK_CONTEXT`, `TECH_SIGNAL`, `LINK_SHARE`, `RISK_PROMO_REFERRAL`, `RULES_ONBOARDING`), build deterministic daily clusters, and add `recommendedAction` for materials. Ran report for `2026-06-30 03:00 MSK`: `reports/daily-materials-signals-audit-20260630T080436Z.html/.md/.json` initially showed 9 materials, all 9 flagged; 8 signals, 6 flagged. With Adam's approval, soft-deleted P0 rules/onboarding materials via existing public DELETE API: IDs `142`, `143`, `144`, `146`, `147`, `149`, `150`. Re-ran audit: `reports/daily-materials-signals-audit-20260630T080811Z.html/.md/.json`; active window now has 2 materials (`145`, `148`), both still flagged as single-source/single-message review candidates; no P0 rules/onboarding materials remain active in the window. Signals remain unchanged because there is no public signal delete API; 8 signals, 6 flagged. No reprocess/backfill/provider/prompt/TG session changes.
- DAILY MATERIAL/SIGNAL AUDIT TOOL ADDED AND RUN LOCALLY, NOT DEPLOYED: Added read-only Node.js script `scripts/audit-daily-materials-signals.js` for Adam's requested iterative workflow. It collects production materials/signals from public APIs, filters by MSK window, flags quality issues (`RULES_ONBOARDING_MATERIAL`, `SINGLE_SOURCE_MATERIAL`, `SINGLE_MESSAGE_MATERIAL`, `OTHER_SINGLE_SOURCE`, `DUPLICATE_SIGNAL_TITLE_IN_WINDOW`, `RISK_PROMO_REFERRAL_*`, etc.), and writes Markdown/JSON reports to `reports/`. Ran `node .\scripts\audit-daily-materials-signals.js --since-msk "2026-06-30 03:00"`; outputs: `reports/daily-materials-signals-audit-20260630T074436Z.md` and `.json`. Findings: 8 materials since 03:00 MSK, 8 flagged; 6 signals, 4 flagged. Report confirmed Adam's issue: 8/8 new materials were single-source/single-message; 6/8 were rules/onboarding materials; duplicate signal titles exist. Added additional backend guard locally: `allowOtherSingleMessageMaterials=0` default and `singleMessageMaterialBlockReason()` blocks rules/onboarding and OTHER single-message materialization before LLM/generation. Full backend `mvn -q test` passed. No deploy, no production row deletion, no reprocess/backfill.
- RULES/WELCOME DUPLICATE SIGNAL AND SINGLE-MESSAGE OVER-MATERIALIZATION FIXES IMPLEMENTED LOCALLY, NOT DEPLOYED: Adam reported duplicate signals for greeting/rules messages and that materials are again based on one message. Added regression coverage and minimal backend fixes. `MessageUsefulnessClassifier` now treats rules-confirmation prompts (`подтвердите`, `ознакомились с правилами`) and group rules text (`правила группы`, pinned-message rules text) as `LOW_VALUE` reject, preventing single-message material candidates. `KnowledgeSignalService` now applies the same rules/onboarding suppression before persisting rejected-message signals, preventing duplicated welcome/rules knowledge signals. Added `preferDiscussionOverSingleMessage` pipeline setting default `1`; when a single-message candidate's source message is already covered by a discussion-segment candidate, legacy LLM/materialization records the single candidate as a ledger loser/retained context and skips one-message generation for it. This is a behavior change intended to prefer multi-message evidence without enabling the new decision-core active path. Verification: targeted `mvn -q "-Dtest=MessageUsefulnessClassifierTest,KnowledgeSignalServiceTest,SemanticDecisionCoreObjectTest" test` passed; full `mvn -q test` passed. No production deploy, no deletion of existing duplicate signals/materials, no reprocess/backfill, no provider/prompt/config/TG session changes.
- DECISION CORE PHASE 1 IMPLEMENTED LOCALLY, NOT DEPLOYED: Adam requested a full backend/ML-pipeline architecture change from scattered stage-level decisions to a unified decision core and asked to continue immediately. Implemented the safe first step only: schema-only shadow storage plus DTO/test skeletons, with no production deploy and no runtime behavior change. Added Flyway `backend/2.0/src/main/resources/db/migration/V29__decision_core_shadow_schema.sql` creating `semantic_decision_objects`, `semantic_decision_observations`, `candidate_decision_ledger`, `context_retained_nodes`, `message_relation_edges`, `link_enrichment_jobs`, `link_enrichment_results`, `dedupe_identities`, `claim_units`, `event_identities`, `candidate_rankings`, `safety_gate_results`, `signal_promotion_runs`, `material_update_candidates`, and `quality_metrics_snapshots`. Added DTOs/tests under `backend/2.0/src/main/java/com/larbcorp/neuroinfogrinder2/decisioncore` and `backend/2.0/src/test/java/com/larbcorp/neuroinfogrinder2/decisioncore`. Saved the full rollout plan to `.docs/plans/pipeline/decisioncore/decision-core-rollout-plan.md`. Verification: targeted `mvn -q -Dtest=SemanticDecisionCoreObjectTest test` passed; full `mvn -q test` completed successfully (existing TDLib failure-isolation test logs an expected stack trace). No LLM routing, prompts, materials, signals, Telegram session/proxy, reprocess/backfill, provider config, or production DB state was changed.
- ZAUR CONTROLLED REPLAY CHECK COMPLETED ON PRODUCTION: Adam asked to take the attached mixed batch of promo/risk/resource messages and push it through the current pipeline again, while first judging manually what should become materials. A temporary uploaded dataset `12242` (`ZAUR controlled batch 2026-06-30`, 42 messages) was created via `/api/v2/datasets/upload`, then a full replay was started; the first public request timed out behind nginx, but backend logs confirmed the run continued and completed as `runId=12263` on `2026-06-29`. Final replay summary: `status=COMPLETED`, `total_messages=42`, `processed_messages=42`, `provider_calls_total=20`, `estimated_cost_usd=0.180280`, `knowledge_items_total=4`. Good result: newly tightened single-message safety filters worked on the dangerous sample set; messages were rejected with reasons including `RISK_SENSITIVE_MANUAL_ONLY`, `ABUSE_OR_FRAUD`, `PROMO_ALONE`, `NEEDS_LINK_ENRICHMENT`, `LOW_VALUE`, and `SUPPRESSED_BY_CLASSIFIER`, including the UPI/SMS/VPN bypass item and the Spotify BIN/CVV dump. Replay also showed the system now really forms clusters/chains on this dataset (`19 embeddings`, `2 microclusters`, `2 macroclusters`, `12 single-message candidates`, `5 directReady` seen in backend logs), so it is no longer purely one-message-only in this scenario. Remaining quality problems found in the same production run: generated materials were only partially correct. Created knowledge items were `132` REFERENCE (macrocluster merging `Duet.so`, `Literouter`, `Langdock`, `PrismaticAPI`), `133` REFERENCE (macrocluster merging `Unlucid.ai` + `Veoaifree`), `134` REFERENCE (`Together AI Chat`), and `135` ANSWER (`Relay.app`). Manual judgment: `134` and `135` are acceptable drafts, `132` is too over-merged/noisy and should ideally be split, and `133` should not exist as a material because it is sourced from uncensored-media promo noise. Important pipeline bug: `/api/v2/replay-runs/12263/clusters` returned route intelligence for macroclusters `546/547` with recommended route `NO_MATERIAL`, yet materials `132/133` were still generated and stored, so cluster-level route suppression is not being enforced consistently after clustering. Operational quirks also observed: the public replay start request hit nginx `504` while the backend continued processing, and replay summary timestamps/duration look wrong (`started_at == finished_at`, `duration_ms=0`) despite backend logs showing the run actually finished around `23:22:13Z`.
- SINGLE-MESSAGE LOCAL CONTEXT + COMPACT GUIDE PROMPT DEPLOYED: Adam asked to keep fast single-message guide extraction, but make guides shorter and more accurate, and to consider nearby chain context instead of judging one message in isolation. Backend-only changes were implemented and deployed. `ReplayV2Service.promptSingle(...)` now includes `localContext` from nearby same-scope messages within a 10-minute window, so the single-message LLM Judge/Generation prompt can see whether the message is standalone, part of a chain, promo noise, or risk content. `generationStyleInstructions()` now explicitly prefers compact materials and tells GUIDE generation to stay as a mini-guide/checklist (usually 2-5 short sections or 4-8 short bullets, normally under ~220 words unless evidence clearly requires more). `MessageUsefulnessClassifier` was also tightened against clear abuse/circumvention patterns from Adam’s sample pack: card/BIN/CVV dumps now reject as `ABUSE_OR_FRAUD`, and SMS/VPN/trial/access-token bypass flows reject as `RISK_SENSITIVE_MANUAL_ONLY` instead of becoming guide candidates. Targeted tests (`MessageUsefulnessClassifierTest`, `ReplayV2ServiceDiscussionSegmentTest`) and full backend `mvn -q test` passed. Production deploy used `.\scripts\deploy-fast.ps1 -Target backend`; only `backend` was rebuilt/recreated, and `postgres/redis/tor/ssh-socks` were not restarted. No reprocess or DB content mutation was performed as part of this prompt/classifier change.
- LIVE BATCH SCOPE FIX DEPLOYED + OLD MATERIALS/SIGNALS/GUIDES DELETED: Adam asked to investigate why guides/materials/signals were polluted, why everything was generated from single messages, why there were no chains/datasets/clusters despite ~18k messages, and then delete old materials/signals/guides. Read-only production SQL/API audit confirmed the active store was entirely fragmented historical output: `knowledge_items deleted_at is null = 35`, all `source_cluster_type=SINGLE_MESSAGE`; `knowledge_signals=71`; `knowledge_signal_topics=79`; `/api/v1/materials` showed only single-message DRAFTs; `/api/v2/pipeline/live/batches` and replay runs `12009/12010/12011` showed live batches of only `1/2/3` messages with terminal reasons `NO_MATERIAL_CANDIDATES` / `LLM_REJECTED_ALL`. Root cause found in live batching code: `AutoPipelineService.onRawMessageStored(...)` and `PipelineLiveService` backfill/requeue paths were creating/looking up `auto_pipeline_batches` by the raw message `topicId` even when the effective auto-pipeline scope was only chat-level, so one chat was split into many per-topic micro-batches and almost never accumulated enough same-run messages for clustering/discussion. Local fix implemented and deployed backend-only: chat-level auto-pipeline now uses `topicId=null` as the batch scope unless a real topic-specific setting exists, while preserving the original message topic on queue rows. Added regression test `AutoPipelineServiceTest.onRawMessageStoredUsesChatLevelBatchScopeForTopicMessage`; targeted `AutoPipelineServiceTest` and full backend `mvn -q test` passed. Production deploy used `.\scripts\deploy-fast.ps1 -Target backend`; only `backend` was rebuilt/recreated, and `postgres/redis/tor/ssh-socks` were not restarted. After Adam’s explicit approval, old content was cleaned directly in production DB: all active `knowledge_items` were soft-deleted (`deleted_at now`, `deleted_by=1`, `deleted_reason='USER_REQUEST_DELETE_OLD_MATERIALS_2026_06_30'`, `status='DELETED'`) and all `knowledge_signals` were deleted; FK cascade removed `knowledge_signal_topics`. Verification after cleanup: `active_materials=0`, `knowledge_signals=0`, `knowledge_signal_topics=0`; public `/api/v1/materials` now returns empty with zero counts and `/api/v1/topics` shows all topics with `materialCount=0`, `signalCount=0`. Caveat: existing `/api/v2/pipeline/live/batches` history still shows pre-fix one-message batches; post-fix accumulation needs fresh incoming messages to verify in production.
- MATERIALS/SIGNALS NOISE + TRACE FIXES IMPLEMENTED LOCALLY: Adam asked to inspect the latest guides/materials/signals, verify whether they belong there, explain repeated errors, why pipeline trace is not visible via UI, and why many duplicate/welcome-style messages end up in materials/guides/signals. Read-only evidence from `reports/materials-and-signals-20260629.md` and `reports/materials-and-signals-snapshot-20260629.json` showed recurring bad outputs: many single-message DRAFT materials and signals created from repeated onboarding/welcome/community-helper posts (`Добро пожаловать`, `Помощник ОМ`, `путеводитель по сообществу`, `база знаний`, lists of chats/support/контент). Root causes found and fixed locally: `MessageUsefulnessClassifier` had no early suppression for community welcome/onboarding templates, `KnowledgeSignalService` had no independent guard against storing the same template as a signal, and `/groups` deep links were inconsistent so trace/message preselection often failed in UI. Implemented local fixes: `MessageUsefulnessClassifier` now rejects community welcome/onboarding templates as `LOW_VALUE`, `KnowledgeSignalService.shouldStore` also suppresses them and its `appMessageUrl` now points to `/groups?chatId=...&message=<rawId>&rawId=<rawId>`, `KnowledgeMaterialService` and `Stage1ReadService` now generate the same raw-id-based `/groups` link, and frontend `groups-chat-view.tsx` now preselects by `message` or `rawId` or legacy `messageId`. Added/updated regression tests for welcome-template rejection and app-message URLs; targeted backend tests passed (`MessageUsefulnessClassifierTest`, `KnowledgeSignalServiceTest`, `KnowledgeMaterialServiceTest`, `Stage1ReadServiceMessagePipelineDetailTest`) and frontend `npm run build` passed. No deploy and no production data cleanup were performed yet, so existing bad materials/signals remain until Adam explicitly approves production mutation/reprocess/delete.
- MATERIALS/SIGNALS EXPORT REPORT CREATED: Adam requested a beautiful Markdown document with all found materials/signals plus sources/info. Generated read-only production API export: `reports/materials-and-signals-20260629.md` and raw snapshot `reports/materials-and-signals-snapshot-20260629.json`. Snapshot captured 18 active DRAFT materials, 23 signals, and 10 knowledge topics. Report includes topic table, material index, full material bodies, source messages, how-built steps, provider calls, trace stages, detailed signals with source info, and completeness notes. No DB writes, edits, deletes, reprocess, provider calls, material generation, publish, deployment, or Telegram/session/proxy actions were performed.
- GUIDE CLASSIFIER FIX DEPLOYED: Adam sent `Как проверить OpenAI-compatible API...` and it became material `92` with `artifact_type=ANSWER`. Root cause: `MessageUsefulnessClassifier` `directAnswer` branch ran before guide/checklist detection because the text contained `Как проверить` plus `ответом GET /models`, so `requiredArtifactType=ANSWER` and LLM complied. Added red regression `HowToCheckOpenAiCompatibleApiClassifiesAsGuideMaterial` (failed `ANSWER`), then added a `checkGuide` branch before direct-answer classification for `как проверить`/ordered checks/API terms. Targeted tests and full backend `mvn -q test` passed; backend-only deploy succeeded; production health/public URL OK. Future equivalent messages should persist as `GUIDE`. Existing material `92` remains `ANSWER` unless explicitly deleted/reprocessed/edited.
- SIGNAL CARD UI FIX + TEST OUTPUT CLEANUP DONE: Adam reported the Signals card showed `3 материалов 0 сигналов`; root cause was `MaterialsPage.TopicCard` always rendering both material and signal badges in the `SIGNALS` tab. Frontend now passes `signalsOnly` in signal-map mode, hides the material badge there, and changes copy to `Темы сигналов` / `Карта сигналов`. Frontend `npm run build` passed and frontend-only deploy succeeded; production health/public URL OK. Test outputs from final NIGTOP run were removed: materials `87-91` soft-deleted via API; signals `44/45` and their topic links physically deleted. Verified `active_materials=0`, `knowledge_signals=0`, `knowledge_signal_topics=0`.
- TDLIB LIVE AUTO-START FIX DEPLOYED: Adam reported messages only started loading after opening the site. Root cause confirmed: pipeline scheduler was ticking, but TDLib stayed stopped after backend restart until UI/API calls invoked `ensureReady()`. Added backend `TdlibLiveUpdateSupervisor` with startup and 60s keepalive for only `CONNECTED`/`READY` accounts; it skips disabled/auth-required accounts and does not call chat sync, topic sync, backfill, history pull, reprocess, provider calls, or material generation. Red test failed first; targeted supervisor test passed; full backend `mvn test` passed `128/128`; backend-only deploy healthy. Production evidence: only backend recreated; frontend/model-worker/postgres/redis/ssh-socks/tor stayed up; diagnostics show `tdlibClientAlive=true`, `proxyConfiguredInTdlib=true`, `proxyPingStatus=OK`; raw messages advanced and live-auto runs started/completed after restart without opening UI.
- NIGTOP CONTROLLED TOPIC CHECK PASSED: Adam asked to run the prepared test messages and verify all scenarios/topics. Production was cleaned to `active_materials=0` and `knowledge_signals=0`, then controlled JSONL `NIGTOP-S01..S10` was run. Backend fixes were implemented/deployed during validation: generated materials now populate `knowledge_item_topics`; explicit status summaries stay `SUMMARY`; safe reference wording no longer false-flags abuse risk; risk/fraud detection covers rate-limit bypass/account farming/referral/anti-abuse patterns; missing `MessageUsefulnessBenchmarkService` was restored for existing endpoint/test; duplicate canonicalization plus duplicate-row skip prevents duplicate materialization. Final dataset `8400`, run `8420`, status `COMPLETED`, `10/10`, `providerCallsTotal=10`, `error=null`. Active outputs left for UI review: materials `87 GUIDE`, `88 GENERATION`, `89 ANSWER`, `90 SUMMARY`, `91 REFERENCE`; signals `44 LINK_ONLY/free-tokens-quotas`, `45 RISK_OR_REFERRAL_SIGNAL/abuse-risk+outages-limits`; duplicate `S10` has `llm_skip_reason=DUPLICATE` and no material. Targeted tests and full backend `mvn -q test` passed; backend-only deploys healthy; report: `reports/nigtop-controlled-topic-check-20260629.md/.json`.
- TEXT CLASSIFICATION / ACCURACY AUDIT COMPLETED READ-ONLY: traced the full raw-text -> rules/bootstrap -> BGE -> clusters/single/discussion -> classical route -> LLM Judge -> DRAFT path, ran current backend `mvn test` (`117/117`) and production read-only checks. Production is healthy with fresh raw gap `1`; worker has real `BAAI/bge-m3` 1024-d embeddings and a narrow semantic sanity check passed. However classical ML is not trained (`trainedRegistry={}`, 0 strong/human-reviewed examples, all 11,833 training rows unassigned); `/classical-ml/models` and `/metrics` expose intended/hard-coded models/F1 as `SUCCESS`; 998/1,308 classical traces abstained and only 13 required Judge. Settings/control mismatches exist: the layer called shadow can hard-block, displayed semantic threshold `0.62` executes as `0.66`, several settings are unused, and live config budget 10/$1 is reconstructed with ReplayRequest defaults 50/$2 (no observed run exceeded 4 calls). Unique configured Judge requests succeeded 115/152 (75.7%). All 38 historical knowledge items are currently soft-deleted, active=0, so current end-to-end material precision is not measurable. No deploy/restart/reprocess/provider call/settings/prompt/DB write was performed. Research: `.docs/research/pipeline/text-classification/system-accuracy-research.md`.
- ARTIFACT TYPE GATE FIX DEPLOYED: backend now owns and enforces required material artifact type across replay generation. Added classifier branches for `GENERATION`, `ANSWER`, `SUMMARY`; single-message candidates carry `requiredArtifactType`; prompts require returned `artifactType` to match; generated JSON is sanitized before `knowledge_items` insert and preserves mismatched provider type as `providerArtifactType`; semantic clusters are split by material bucket; reject-shaped generation payloads are not stored as normal material; duplicate-like source text is filtered from macro material sources. Full backend `mvn -q test` passed and backend-only deploy passed. Controlled production run `8258` over temporary dataset `8239` created correct DRAFT materials: `63 GUIDE`, `64 GENERATION` (provider originally `OTHER`, backend corrected), `65 ANSWER`, `66 SUMMARY`, `67 REFERENCE`; duplicate did not create material or pollute sources. Cleanup completed: old test materials `37/38` removed; controlled materials `54-67` soft-deleted; dataset `8239` deleted; final counts `active_old_nigtest=0`, `remaining_dataset=0`, `remaining_runs=0`, `remaining_messages=0`. Report: `reports/artifact-type-gate-check-20260629.md/.json`.
- CONTROLLED 10-SCENARIO CHECK DONE: Adam approved expanding topic words and running the full 10-scenario plan. Deployed tighter signal routing, signal source/open-chat fixes, non-material prefilter before BGE clustering, and hard-route risk signal persistence. Local backend `mvn test` passed `113/113`; frontend build passed; backend/frontend health OK. Production controlled dataset `8136` and runs `8151-8153` were executed, then fully cleaned up. Final run `8153` showed link-only/promo/risk/low-value behavior fixed, but material artifact typing remains weak (`GENERATION/ANSWER/SUMMARY` misclassified) and macro clustering still over-merges guide/reference/duplicate. Report: `reports/controlled-10-scenario-pipeline-check-20260628.md/.json`.
- TEST SIGNAL CLEANUP DONE: Adam approved deleting only test `NIGTEST` knowledge signals. Deleted production `knowledge_signals` ids `1,12,13,14,15,16,17,18,19,20` with title prefix `[NIGTEST-A10]`; FK cascade removed their topic assignments. Did not touch raw/dataset messages and did not delete normal `LINK_ONLY` signals. Verification: `remaining_nigtest=0`, unique `knowledge_signals=10`, public `/api/v1/topics` sum `signalCount=23`; remaining `LINK_ONLY` signals stay as `NEEDS_LINK_ENRICHMENT` for future link-enrichment engine.
- NIGTEST LIGHTWEIGHT MATERIAL GATE FIX DEPLOYED: Adam asked to make concise useful messages pass. Root cause was the final LLM/material gate: clusters were created but generic route blocks/LLM `reject` for “not enough standalone material” stopped generation. Backend now permits safe lightweight override for narrow checklists/references when there is no hard risk/duplicate/promo/manual-review block, normalizes cached ModelHub judge payloads, uses cluster score as confidence for lightweight overrides, and creates a source-grounded DRAFT fallback if `KNOWLEDGE_GENERATION` times out after an allowed lightweight override. Targeted backend tests passed; backend-only deploys preserved infra. Controlled rerun `8149` over NIGTEST dataset `7999` completed with `error=null` and created DRAFT materials `37` GUIDE (`A02,A06,A07,A08`) and `38` REFERENCE (`A03,A04`). A09/A10 did not become materials; A10 remains signal path.
- MATERIALS SIGNALS FILTER DEPLOYED: Adam asked to keep `Сигналы` in the materials filters and show `Карта знаний` there. Implemented `/materials?type=SIGNALS` as a dedicated knowledge-map mode: the regular materials tabs no longer render a duplicate map, `Сигналы` count is the real summed backend `topic.signalCount` rather than topic count, and the selected Signals view shows one `Карта знаний` card with topic cards and honest per-topic counts. Frontend build and deploy passed; production check returned `topics=10`, `signalCount=27`, `/materials?type=SIGNALS` HTTP 200.
- PIPELINE/MATERIALS UI FIX DEPLOYED: fixed Adam's pasted review for `https://neuroinfogrinder.latrdev.ru/pipeline`. Pipeline stage labels are now Russian (`Входящие сообщения`, `Приём и нормализация`, `Очередь и правила`, `Первичная классификация`, etc.). `Raw messages` now maps to backend `telegram_ingest` instead of falling back to old `/pipeline/results`, so the selected-stage table loads from `/api/v2/pipeline/live/stages/telegram_ingest/messages?status=all&limit=200`; production check returned 10 rows for limit 10 with first id `16328`. Default period filter changed to `Всё время` so table rows are not hidden by today's date. Empty pagination now says `Нет сообщений под выбранные фильтры` instead of `Показано 1-0 из 0`. Production topics API returned `total=10`; frontend build/deploy passed; production health OK.
- LATRDEV PIPELINE TARGET: Adam clarified the canonical URL to check is `https://neuroinfogrinder.latrdev.ru/pipeline`, not the older `neuroinfogrinder.ru` URL. Frontend deploy was re-run and verified against latrdev. During deploy, stale remote-only `frontend/src/pages/signals` source caused a build failure; it did not exist locally, so it was removed from `/srv/neuroinfogrinder/app/frontend/src/pages/signals` and frontend deploy then passed. Final status healthy; latrdev `/pipeline` returned HTTP 200 and current bundle `assets/index-Dz4OQ2Ko.js`.
- PIPELINE ORDER FIX: investigated Adam's question why stage 6 had more messages than stage 5. Backend live counts showed the issue was visual ordering, not data corruption: `single_message_detection.processed=11140`, `embeddings.processed=2508`, `clustering.processed=74`, `llm_judge.processed=147`. The UI previously placed `Embeddings` before `Single-message`, which made the next visual card larger. Reordered visual acceptance funnel to `Run -> Single-message -> Embeddings -> LLM Judge -> Clustering -> Material`, so `Потеря` is always `previous visible card - current visible card` for the displayed chain. Note: backend stage execution still has branches; the UI is an acceptance funnel, not raw execution order.
- PIPELINE UI FIX DEPLOYED: fixed Adam's review findings on `/pipeline` and `/pipeline/stages/:stageId`. Removed duplicate refresh icon button, replaced decorative conveyor filter cards with real styled `<select>` controls, stopped filter overflow with responsive 1/2/3/6-column grid, renamed `Потеря дальше` to `Потеря`, and changed loss calculation to `previous stage value - current stage value`. Conveyor table now loads real stage messages through `/api/v2/pipeline/live/stages/{backendStageId}/messages?status=all&limit=200` for selected stages instead of only replay results. Stage route now maps UI aliases to backend stages (`raw_messages -> telegram_ingest`, `intake -> normalization`, `queue -> rule_signals`, `run -> bootstrap_classification`), so stage details/messages are not all zeros for UI aliases. Stage filters were restyled to match the site and show explicit fallback labels when option sets are empty. Frontend-only deploy completed, production healthy, and production read-only checks showed non-zero data: `embeddings` processed `2505`, skipped `9058`, messages list `20`; `normalization` messages list `20`.
- REAL PIPELINE DEPLOYED: `/pipeline` is now the real message conveyor with live summary/stages/results and SSE invalidation from `usePipelineEvents`; header shows `real-time on` / `polling fallback`. `/pipeline/stages/:stageId` now uses real read-only stage data from `/api/v2/pipeline/live/stages/{stageId}/details` and `/api/v2/pipeline/live/stages/{stageId}/messages`, with working filters for period/status/chat/reason/model/search. Backend `PipelineLiveService.stageMessages` supports read-only `status=all`. Production deploy `./scripts/deploy-fast.ps1 -Target all` completed after fixing `ReplayV2Service` DI with `@Autowired`; final health OK and read-only endpoint checks returned HTTP 200 for live summary, stages, and `embeddings/messages?status=all&limit=5`.
- VERIFICATION: frontend `npm run build` passed; backend `mvn test` passed `98/98`; production backend/frontend/model-worker/postgres/redis/ssh-socks/tor healthy; public URL HTTP 200. First deploy attempt temporarily failed because Spring could not choose among multiple `ReplayV2Service` constructors (`No default constructor found`); explicit constructor injection fixed startup and redeploy succeeded.
- DEPLOYED TOPICS/SIGNALS MVP: deployed current backend+frontend worktree with `scripts/deploy-fast.ps1 -Target all`. Flyway applied `V28__knowledge_topics_and_signals.sql`; `/api/v1/topics` returns 10 seeded canonical topics; `/api/v1/topics/free-tokens-quotas?tab=signals` returns an empty signal list until new matching rejected messages or an approved backfill exists. Backend/frontend healthy, public URL 200, model-worker/postgres/redis/ssh-socks/tor stayed up. No historical backfill/reprocess/materialization was run. Caveat: because the whole current worktree was deployed, parallel classical-ML backend changes also went out while model-worker was not restarted/updated; backend logs show non-fatal `[CLASSICAL_ML_SHADOW_FAILED]` warnings for `/classical-ml/infer/message` HTTP 404. Core app remains healthy.
- LOCAL CLASSICAL ML PHASES NOT DEPLOYED: continued `backend/2.0` classical ML scaffold so worker inference now emits all requested 0-9 stage outputs in trace (`preprocessing`, `meaning`, `valueLevel`, `usefulnessKind`, `evidenceSufficiency`, `assemblyStrategy`, `materialRoute`, `uiReason`, `dedupeCluster`, `llmJudge`). `DefaultRouteIntelligenceEngine` now consumes preprocessing/dedupe/final-gate outputs to block risk-sensitive, duplicate/near-duplicate, and classical final-gate rejected candidates before Judge/generation. Red tests failed first, then targeted ML/replay regression tests passed. No production deploy, DB write, materialization, settings, Telegram/proxy/session, or provider changes were made.
- CLASSICAL ML GOLDEN BATCH: worker training supports real sklearn backends `MultinomialNB`, calibrated `LinearSVC`, `DecisionTreeClassifier`, Logistic Regression, and Random Forest ensembles with averaged probabilities, uncertainty/margin/entropy/modelAgreement, and conservative abstain. Added offline golden runner `scripts/run-classical-ml-golden-batch.py`; generated `reports/classical-ml-golden-batch-20260628.md/.json/.csv`. Golden evidence snapshot classified `GB01-GB08` as expected at stage level (`8/8`), but every row remains `abstained=true` because the offline input is evidence snapshots, not full raw message text. Verification passed before deploy.
- CLASSICAL ML SHADOW DEPLOYED: backend and model-worker were deployed after full local validation. Local checks: full `backend/2.0 mvn -q test` passed, `python -m py_compile app.py` passed, offline golden batch `GB01-GB08` stage classification `8/8` with conservative abstain. Deploy used `scripts/deploy-fast.ps1 -Target backend` then `-Target model-worker`; postgres/redis/tor/ssh-socks stayed up with 8-day uptime and were not recreated. Production checks: backend/model-worker healthy, public URL 200, `/classical-ml/models` shows new stage model slots, `/classical-ml/infer/message` smoke returns all 10 stages plus uncertainty/modelAgreement and `recommendedAction=TRACE_ONLY` due `MODEL_UNCERTAINTY`. Still shadow/advisory; no materialization/settings/provider/Telegram/proxy/session changes.
- CURRENT HANDOFF: working on production-ready pipeline inspection UI. Latest user-visible focus is `/pipeline/messages-mockup` (`frontend/src/pages/pipeline/PipelineMessagesMockupPage.tsx`). The funnel must look like one integrated pipeline scheme, not a separate wave plus cards: thin blue->cyan->green->yellow->orange->purple->teal ribbon above compact stage cards, loss badges between cards, dotted vertical guide lines down to transition boundaries, compact dropdown-like filters, visible summary cards, selected `LLM Judge`, and visible bottom table. Local `frontend npm run build` passed after the latest funnel composition fix. Frontend-only deploy was run and production URL opened: `https://neuroinfogrinder.ru/pipeline/messages-mockup`.
- DEPLOY NOTES: use `scripts/deploy-fast.ps1`; prefer `-Target frontend` for mockup/UI-only changes, `-Target backend` for backend-only, `-Target all` only when both changed. Always check `./scripts/deploy-fast.ps1 -Target status` after deploy. Routine deploy must not restart postgres/redis/tor/ssh-socks. Do not print `/srv/neuroinfogrinder/app/.env` or secrets. Production app path is `/srv/neuroinfogrinder/app`; SSH key is `C:\Users\Adam\.ssh\neuroinfogrinder_ru_vps`.
- LOCAL RUN NOTES: frontend dev server can run with `cd frontend; npm run dev -- --host 127.0.0.1 --port 5173`, but it expects backend at `localhost:8080`. Local backend needs local Postgres from `backend/2.0/docker-compose.local.yml`; Docker Desktop was not running in the latest session, so local backend could not start. For viewing authenticated UI, production backend + frontend deploy was used.
- PARTIAL TASK WARNING: message pipeline inspector drawer on `/groups` was partially implemented/deployed before the latest mockup fix. Backend endpoint `/api/v1/messages/{rawId}/pipeline-detail`, frontend `message-detail-panel`, backend tests, frontend build, and production API spot checks for material sources/rejected examples were done. Required final reports `reports/message-pipeline-drawer-20260626.md/.json` are still not completed in this handoff unless a later commit adds them. Do not claim that drawer task is fully accepted until reports and browser checks are completed.
- Frontend TASK 12-15 follow-up is deployed frontend-only. Sidebar now includes the new pipeline mockup entries: `/pipeline/messages-mockup` (`Фаннел сообщений`), `/pipeline/clusters-map-mockup` (`Карта кластеров`), and `/pipeline/stages/embeddings-bge-m3` (`Embeddings / BGE-M3`). `Материалы` and `Промпты` now use distinct sidebar icons (`IconBook2` vs `IconBraces`). The yellow `Добавить провайдера` button was removed from the AI providers page header; the add-provider card remains in the grid. Local `frontend npm run build` passed, `scripts/deploy-fast.ps1 -Target frontend` completed, frontend health OK, production status healthy, and public URL returned HTTP 200. No backend, DB, prompt, provider, material, Telegram, proxy, model-worker, postgres, redis, tor, or ssh-socks changes were made.
- TASK 11 prompt rewrite is prepared as preview/report artifacts only: `reports/knowledge-generation-prompt-preview-20260627.md/.json`. It proposes a safer `KNOWLEDGE_GENERATION` Generation Expert prompt with standalone material style, no meta-chat framing, provider/API caveat, repo/resource caveat, risk-sensitive guard, link-only insufficiency disclosure, and preserved output schema. Production prompt template was not changed and no material was generated.
- Settings/providers/pipeline diagnostics UI changes are included in the frontend deploy: settings copy was clarified, worker health was compacted, cluster detail mockup now includes LLM grouping explanation/raw/parsed response sections, and message detail mockup now includes LLM provider, quality, and source diagnostics decision cards.
- Frontend high-fidelity mockup for cluster detail is implemented locally at `/pipeline/clusters/1287`. It adds `frontend/src/pages/pipeline/PipelineClusterDetailMockupPage.tsx` and a route in `frontend/src/app/router.tsx`. The static screen shows `Кластер #1287` with breadcrumb, metadata strip, source-message table, horizontal cluster pipeline trace, merge rationale, cluster decision, relationship mini-graph, and recommended next step. Frontend `npm run build` passes. No production deploy, API, backend, DB, pipeline, settings, or material changes were made.
- High-fidelity UI mockup for the cluster map overview is implemented locally at `/pipeline/clusters-map-mockup`. It adds `frontend/src/pages/pipeline/PipelineClustersMapMockupPage.tsx` and updates `frontend/src/app/router.tsx`. The route uses the existing authenticated AppShell/sidebar and current beige/cream NeuroInfoGrinder styling: breadcrumb/header, six filters, five metric cards, large bubble cluster map, legend for size/topic/result, selected cluster side panel, and `Последние кластеры` table. `cd frontend && npm run build` passes. No production deploy, API, backend, DB, settings, pipeline data, or material changes were made.
- Frontend high-fidelity mockup for pipeline message detail is implemented locally at `/pipeline/messages/4743`. It adds `frontend/src/pages/pipeline/PipelineMessageDetailMockupPage.tsx`, a route in `frontend/src/app/router.tsx`, and links raw `4743` from the Embeddings stage mockup table. The screen is static/mock data only: source message card, current state/score/relations cards, and a full vertical pipeline timeline with expanded LLM Judge rejection details and collapsed prompt/response/JSON panels. Frontend `npm run build` passes. No production deploy, API, backend, DB, pipeline, settings, or material changes were made.
- High-fidelity UI mockup for the full message pipeline funnel is implemented locally at `/pipeline/messages-mockup`. It adds `frontend/src/pages/pipeline/PipelineMessagesMockupPage.tsx` and updates `frontend/src/app/router.tsx`. The route uses the existing authenticated AppShell/sidebar and current beige/cream NeuroInfoGrinder styling: header/buttons, six compact filters, summary metric cards, narrowing color funnel with loss badges, 10 clickable-looking stage cards with selected `LLM Judge`, hint text, and `Сообщения этапа: LLM Judge` table with chips and pagination. `cd frontend && npm run build` passes. No production deploy, API, backend, DB, settings, pipeline data, or material changes were made.
- Frontend high-fidelity mockup for pipeline stage detail is implemented locally at `/pipeline/stages/embeddings-bge-m3`. It adds `frontend/src/pages/pipeline/PipelineStageEmbeddingsMockupPage.tsx` and a route in `frontend/src/app/router.tsx`. The screen is static/mock data only, styled in current beige/cream NeuroInfoGrinder UI with summary cards, filters, tabs, and a message-stage table for `Этап: Embeddings / BGE-M3`. Frontend `npm run build` passes. No production deploy, API, backend, DB, pipeline, settings, or material changes were made.
- Final controlled DISCUSSION_SEGMENT production 24h report is complete and read-only. Reports `reports/discussion-segment-controlled-production-24h-20260627.md/.json` now have `Status: FINAL`, captured at `2026-06-27T18:19:28Z` after final eligibility `2026-06-27T16:23:22Z`. Snapshot: `1175` segments after enable, `0` scorer accepted after enable, `13` generation calls (`9` success, `4` timeout/error), `0` DRAFT and `0` PUBLISHED DISCUSSION materials after enable, active DISCUSSION materials remain only pre-enable `23/24/25`, active knowledge items `29`. Production services/public URL healthy. No settings/materials/provider config/scoring/requeue/backlog/Telegram/proxy changes were made.
- Controlled LLM Judge dry-run for MessageUsefulnessClassifier candidates is complete. Reports: `reports/message-usefulness-llm-judge-dry-run-20260627.md/.json`, `reports/message-usefulness-llm-judge-calls-20260627.csv`, `reports/message-usefulness-llm-judge-vs-classifier-20260627.csv`. Current task baseline active materials was `29` (previous known `30` had already changed before the task); post-check stayed `29`, total `knowledge_items=33`, active DISCUSSION materials `3`, dry-run run `6601` had `6` judge calls, `0` KNOWLEDGE_GENERATION calls, and `0` knowledge_items. raw `11142` accepted as `NEWS_REFERENCE_MATERIAL_CANDIDATE`/`REFERENCE` confidence `0.78`; raw `11484` accepted as `RESOURCE_REFERENCE_MATERIAL_CANDIDATE`/`REFERENCE` confidence `0.74`; raw `11193` stayed pre-gate rejected `LINK_ONLY/NEEDS_LINK_ENRICHMENT` with no LLM call; raw `10172`, `12038`, `11434`, `8447` got provider HTTP `502` and are inconclusive. Health/public URL/logs clean.
- MessageUsefulnessClassifier Option B is implemented and deployed backend-only. Added internal `MessageUsefulnessClassifier` and `MessageUsefulnessResult`, called from existing `ReplayV2Service.singleMessageDetection`; no parallel router/pipeline. It supports `PRACTICAL_GUIDE`, `NEWS_UPDATE`, `RESOURCE_REFERENCE`, `STATUS_OUTAGE`, `QNA`, `LINK_WITH_CONTEXT`, `LINK_ONLY`, `PROMO_ALONE`, `ABUSE_OR_FRAUD`, `ACCESS_CIRCUMVENTION`, `ENTITY_ONLY`, and `LOW_VALUE`. Output is stored in existing `single_message_score_breakdown_json.usefulnessClassification`, added to single-message signals and LLM judge context, and only affects candidate route/proposed material type/score dimension/rejection reason. Existing LLM judge, generation, dedupe, caps/safety, DISCUSSION_SEGMENT, cluster logic, settings, provider config, global threshold, Telegram session, and public-discovery guards remain authoritative/unchanged. Tests: targeted classifier `10/10`, full backend `80/80`. Reports: `reports/message-usefulness-classifier-20260627.md/.json`, `reports/message-usefulness-classifier-tests-20260627.csv`, `reports/message-usefulness-classifier-dry-run-20260627.csv`.
- `/materials` count mismatch is fixed and deployed backend+frontend. Root cause: six active DRAFT materials had legacy/unknown `artifact_type` values (`cluster_summary` x3, `newsBrief`, `instruction`, `REFERENCE`) that were counted in `Все` but not in visible type tabs. Backend `/api/v1/materials` now normalizes list/count/filter types to `GUIDE`, `GENERATION`, `ANSWER`, `SUMMARY`, `OTHER` and returns `counts.total/byType/byStatus`; frontend shows Russian labels `Гайды`, `Генерации`, `Ответы`, `Сводки`, and `Другое` only when count > 0. Production API after deploy: total `30`, byType `GUIDE=11`, `GENERATION=9`, `ANSWER=1`, `SUMMARY=3`, `OTHER=6`; `contentType=OTHER` returns ids `32,21,19,17,16,13`. Tests: targeted material tests `4/4`, full backend `70/70`, frontend build passed. Reports: `reports/materials-counts-consistency-20260627.md/.json`, `reports/materials-counts-audit-20260627.csv`.
- UI-visible message source recovery for `API SUPPORT | ModelHub` (`telegram_chat_id=-1003898985313`, group id `38`) is complete and read-only. `/api/v1/groups/38/messages?page=0&size=300&sort=messageDate,desc` was inspected; backend reads `raw_messages` and `MessageDto.id` equals `raw_messages.id`. Recovered `U01/U04` and U02-equivalent GPT-5.6 message as raw `11142` (`raw_messages.caption`, dataset `10121`, replay message `7397`, run `5055`, score `0.51`, rejected `LOW_SINGLE_MESSAGE_SCORE`, embedded, no DISCUSSION/LLM/material). Recovered `U03` lolz.live as raw `11193` (`raw_messages.text`, dataset `10168`, replay message `7444`, run `5092`, score `0`, rejected `TOO_SHORT`, embedded, no DISCUSSION/LLM/material). Reports: `reports/ui-visible-message-source-recovery-20260627.*` and `reports/expected-message-source-recovery-20260627.*`. Post-check confirmed settings/material counts unchanged.
- Read-only final/interim controlled production + overnight expected-material audit is complete. Required reports were created: `reports/discussion-segment-controlled-production-24h-20260627.md/.json`, `reports/overnight-expected-materials-audit-20260627.md/.json`, `reports/overnight-expected-materials-audit-messages-20260627.csv`, and `reports/overnight-expected-materials-audit-groups-20260627.csv`. Snapshot was captured at `2026-06-27T09:55Z`, so it is `INTERIM_NOT_FINAL`; final eligible time remains `2026-06-27T16:23:22Z`. No settings/materials/prompts/caps/reprocess/Telegram/proxy/publish changes were made.
- Overnight specified window `2026-06-27 03:40-04:15 MSK` mapped to `2026-06-27T00:40-01:15Z`. Production had `28` raw rows, `28` dataset rows, `28` run-message rows, but exact expected M01-M12 content matched `0/12` in that window; nearest timestamp rows were traced and broader-day keyword candidates were recorded where found. Window DISCUSSION_SEGMENT rows `0`, provider calls `0`, materials `0`; non-materialization in the specified window was not daily-cap related.
- Controlled production interim snapshot after enable: `948` post-enable discussion segments, `0` DRAFT/PUBLISHED DISCUSSION materials after enable, `3` active DISCUSSION materials total (`23/24/25`, pre-enable manual controlled materialization), `0` DISCUSSION materials on `2026-06-27 UTC`. Skip/provider evidence includes provider-not-configured/timeouts and `2` daily-limit skips outside the specified window; final 24h report must be retaken after `2026-06-27T16:23:22Z`.
- Material style/source-transparency fix is deployed. Future `KNOWLEDGE_GENERATION` prompts now forbid meta-chat framing, require standalone GUIDE/ANSWER/SUMMARY/REFERENCE structure, preserve only source facts, block unsupported exact API/pricing/limit/SLA/provider/legal/medical/security claims, and require the provider-doc caveat for external API/provider behavior. `/api/v1/materials/{id}` now exposes ordered source messages, discussion segment metadata, how-built steps, provider previews with redaction, and trace/provider diagnostics. `/materials/{id}` now shows `Источники материала`, ordered source cards, `Как был составлен материал`, and `Trace / provider calls`; null/zero durations show `нет данных`. Tests: targeted `18/18`, full backend `67/67`, frontend build passed. Reports: `reports/material-style-source-transparency-20260626.md/.json`.
- Read-only verification for materials `23/24/25`: each remains `DISCUSSION_SEGMENT`, `sourceCount=6`, `sourceMessages=6`, `howBuiltSteps=5`, `traceStages=114`, `providerCalls=1`; browser checks confirmed ordered sources, trace/provider blocks, no old `Исходное сообщение` label, and no fake `0 мс`. Existing bodies still contain old meta-chat phrases (`23/24`: `участники обсуждения`, `в обсуждении`; `25`: `в обсуждении`), so regenerate/manual edit is recommended only after explicit approval. No existing body was rewritten and no new materials were generated.
- DISCUSSION_SEGMENT controlled production mode is enabled with safe gates. Effective production settings: `discussionSegmentGenerationEnabled=1`, mode `CONTROLLED`, DRAFT-only, fresh-only from `2026-06-26T16:23:22Z`, daily cap `3`, per chat/topic/day cap `1`, LLM-accepted required, risk-sensitive skip enabled, provider/generation error stop enabled. No backlog/reprocess was run. Initial monitor snapshot at `2026-06-26T16:38Z`: post-enable detected segments `0`, judge calls `0`, generation calls `0`, DRAFT materials after enable `0`, PUBLISHED after enable `0`; active DISCUSSION_SEGMENT materials remain pre-enable controlled materials `23/24/25`. Reports: `reports/discussion-segment-controlled-mode-enable-20260626.md/.json`, `reports/discussion-segment-controlled-production-24h-20260626.md/.json`, raw monitor `reports/discussion-controlled-production-monitor-raw-20260626.json`.
- Backend prompt/guard fix for controlled production is deployed backend-only. Generation prompt now requires the provider/API/cache/pricing/rate-limit/model-availability caveat: `Проверьте актуальную документацию провайдера: лимиты, тарифы, поведение cache и доступность моделей могут меняться.` Full backend tests passed `62/62`; production backend/frontend/model-worker/postgres/redis/ssh-socks/tor are healthy and public URL returns `200`.
- Controlled materialization test for exactly three DISCUSSION_SEGMENT candidates is complete: S0024/E01 -> material `23` GUIDE DRAFT, S0269/E02 -> material `24` GUIDE DRAFT, S0132/E04 -> material `25` SUMMARY DRAFT. No global generation was enabled (`discussionSegmentGenerationEnabled=0`), no backlog/reprocess was run, and no unrelated segments were materialized. Each material has `candidateType=DISCUSSION_SEGMENT`, segment ids `118/119/120`, 6 ordered source messages, trace stages, and one `KNOWLEDGE_GENERATION` provider call (`107/108/110`). API/detail checks passed and public routes `/materials`, `/materials/23`, `/materials/24`, `/materials/25` returned `200`. Reports: `reports/discussion-segment-controlled-materialization-20260626.md/.json`, `reports/discussion-segment-controlled-materialization-materials-20260626.csv`, `reports/discussion-segment-controlled-materialization-review-20260626.csv`.
- Controlled materialization quality verdicts: material `23` S0024 GOOD; material `24` S0269 OK_NEEDS_MINOR_PROMPT_TWEAK because cache timing/tariff behavior needs stronger doc-verification caveat; material `25` S0132 GOOD. Post-check: `DISCUSSION_SEGMENT` materials after `3`, active knowledge_items `24`, controlled runs `3`, controlled generation calls `3`; backend logs clean and production health/public URL OK.
- Manual review of the 6 accepted tightened DISCUSSION_SEGMENT judge outputs is complete. Reports: `reports/discussion-segment-accepted-review-20260626.md/.json` and `reports/discussion-segment-accepted-review-table-20260626.csv`. Reviewed all 6 accepted segments and 36 source messages. Verdicts: `S0132` and `S0217` approved for controlled materialization; `S0024` approved after merge with `S0021`; `S0269` approved after merge with `S0266`; `S0021` and `S0266` rejected as duplicate standalone materials but useful as merge context. E07/S0217 safety is acceptable only as cautious anecdotal/source-grounded SUMMARY with no medical claims. Safety re-check stayed `discussionSegmentGenerationEnabled=0`, discussion materials `0`, discussion generation calls `0`.
- Tightened DISCUSSION_SEGMENT LLM judge dry-run is complete for exactly 10 selected accepted tightened segments (`S0024`, `S0269`, `S0132`, `S0217`, `S0021`, `S0266`, `S0222`, `S0135`, `S0109`, `S0112`). Attempted model calls `10`; persisted provider call rows `6`; provider errors `4` were explicit HTTP `502`/parse-error rows in the report. LLM accepted `6`, rejected/errored `4`; material types among accepted: `GUIDE=4`, `SUMMARY=2`; scorer/LLM type mismatches `0`; parsed safety/hallucination issues `0`. Reports: `reports/discussion-segment-tightened-llm-judge-20260626.md/.json`, `reports/discussion-segment-tightened-llm-judge-calls-20260626.csv`, `reports/discussion-segment-tightened-llm-judge-vs-scorer-20260626.csv`.
- Tightened judge safety checks: pre-check `discussionSegmentGenerationEnabled=0`, `DISCUSSION_SEGMENT` materials `0`, active `knowledge_items=21`, discussion `KNOWLEDGE_GENERATION` calls `0`; post-check `discussionSegmentGenerationEnabled=0`, `DISCUSSION_SEGMENT` materials `0`, discussion `KNOWLEDGE_GENERATION` calls `0`, public URL `200`, backend logs clean. A new unrelated active material `22:SINGLE_MESSAGE` appeared after baseline; no DISCUSSION_SEGMENT material was created.
- DISCUSSION_SEGMENT tightening is implemented, tested, deployed backend-only, and verified without generation/materialization. Backend now applies stricter scoring, risk/context rejection, overlap duplicate suppression, 20-minute bucket caps, and product-feedback SUMMARY calibration. Full backend tests pass `60/60`. Production safety checks after deploy: `discussionSegmentGenerationEnabled=0`, active `DISCUSSION_SEGMENT` materials `0`, backend/frontend health OK, public URL `200`, backend logs clean, infra uptime preserved.
- Tightening dry-run report for the same `2026-06-26 01:00-10:00 MSK` evidence is complete: `reports/discussion-segment-tightening-20260626.md/.json`, `reports/discussion-segment-tightening-comparison-20260626.csv`, `reports/discussion-segment-tightening-segments-20260626.csv`. Candidate windows `363`; accepted before dedupe `60`; accepted after dedupe/limits `17` vs previous `89`; overlap duplicates rejected `38`; window-limit rejected `5`; low-value adjacent rejected `132`; entity-only rejected `10`; needs-more-context `1`; E01/E02/E04/E07 covered; E04 now `SUMMARY`; E08 `DISCUSSION_SEGMENT_NEEDS_MORE_CONTEXT`; E09/E10 rejected.
- LLM judge dry-run for selected DISCUSSION_SEGMENT fixtures is complete. Scope was exactly segment ids `86`, `87`, `50`, `88`, `89`; provider calls `5`; `KNOWLEDGE_GENERATION` calls for discussion `0`; `DISCUSSION_SEGMENT` materials `0`; `discussionSegmentGenerationEnabled` still fallback `0`; production health/public URL OK; backend logs clean. Reports: `reports/discussion-segment-llm-judge-dry-run-20260626.md/.json`, `reports/discussion-segment-llm-judge-calls-20260626.csv`, `reports/discussion-segment-llm-judge-vs-manual-20260626.csv`.
- LLM judge results: E01 segment `86` accepted GUIDE confidence `0.93`; E02 segment `87` accepted GUIDE confidence `0.92`; E04 segment `50` accepted but as GUIDE instead of expected SUMMARY confidence `0.91`; E07 segment `88` accepted careful ANSWER confidence `0.78`; E08 segment `89` rejected `REJECTED_NEEDS_MORE_CONTEXT` confidence `0.86` due missing source question/security-risk context. Acceptance caveat: global active `knowledge_items` increased from `17` to `20` during the interval due unrelated live-auto SINGLE_MESSAGE/MACRO runs `2549`, `2615`, `2618`; no discussion-segment materials were created.
- DISCUSSION_SEGMENT MVP is deployed backend-only to production with generation disabled by default. Flyway `V24__discussion_segments.sql` is applied; `discussion_segments` and `discussion_segment_sources` exist. Production has no `pipeline_settings` row for `discussionSegmentGenerationEnabled`, so backend uses code fallback `0`; final safety query confirmed `DISCUSSION_SEGMENT_JUDGE` provider calls `0`, discussion generation provider calls `0`, and `knowledge_items.source_cluster_type='DISCUSSION_SEGMENT'` count `0`.
- Bounded dry-run for `2026-06-26 01:00-10:00 MSK` completed without backlog/reprocess/materialization. Reports: `reports/discussion-segment-dry-run-20260626.md/.json`, `reports/discussion-segment-dry-run-segments-20260626.csv`, `reports/discussion-segment-dry-run-sources-20260626.csv`, `reports/discussion-segment-dry-run-vs-manual-20260626.csv`. Dry-run found `919` processable rows, `367` candidate windows, `89` accepted diagnostic segments, `278` rejected windows, average source count `5.63`, max source count `11`, persisted `89` diagnostic segments and `501` sources with `DRY_RUN_GENERATION_DISABLED`. Fixture segment ids: E01=`86`, E02=`87`, E04=`50`, E07=`88`, E08=`89`; E09/E10 rejected. `knowledge_items` stayed `17` before/after; discussion materials stayed `0`.
- Manual human-style ground-truth audit for the same overnight window is complete and read-only: `reports/manual-ground-truth-overnight-20260626.md/.json` plus required CSVs. Agent reviewed all 919 grouped overnight messages in context and identified 12 manual classes, 12 manual clusters, 10 expected material candidates, and 8 useful expected materials missed by the current pipeline.
- Manual audit root finding: the main product-quality gap is not raw ingest or queueing; it is lack of discussion/context grouping. 6 of 8 useful expected materials require multi-message chains (`DISCUSSION_SEGMENT`/thread grouping), especially API status/fallback diagnostics, Claude/Anthropic cache limits, Lampa/StreamBert resource context, Singular CLI pain points, biohacking retreat caution, and remote-location risk advice. Single-message false negatives include raw `6536`, `6224`, `6765`, `6105`, and `6543`.
- P0 overnight audit for `2026-06-26 01:00-10:00 MSK` is complete and documented in `reports/overnight-p0-audit-20260626.md/.json`. Production had `919` raw messages, all `919` were enabled/processable and reached intake, queue, batch, run, and replay; `146` got embeddings; `2` became single-message candidates and reached LLM; `0` materials were created.
- Root cause for zero new guides/materials was a safe backend parser bug, not ingest/proxy/provider/duplicate/scoring: raw `6861`/run `1954` received positive LLM judge decision `SINGLE_MESSAGE_MATERIAL_CANDIDATE`, but `approvedJudgeDecision` did not whitelist that value, so `KNOWLEDGE_GENERATION` was skipped and no `knowledge_items` row was written. Raw `6716`/run `1847` was correctly LLM-rejected as a question/topic.
- Safe P0 bugfix is implemented, tested, and deployed backend-only: `approvedJudgeDecision` now accepts `SINGLE_MESSAGE_MATERIAL_CANDIDATE` and `DIRECT_MATERIAL_READY`; regression test `singleMessageMaterialCandidateJudgeDecisionIsApproved` failed red then passed; full backend suite passed `49/49`; production backend health OK and infra uptime preserved.
- Telegram account/session and material deletion changes are implemented and deployed. New schema `V23` adds `telegram_identities`, encrypted/hash phone fields, session soft-delete fields, identity links on chats/raw/topics, and knowledge item soft-delete fields. Production `.env` has generated phone privacy keys with a backup; key values were not printed. Backend/frontend health OK and public URL returns 200.
- Existing Telegram accounts still show masked legacy phones because old DB rows only had `phone_masked`; new/re-added accounts can store encrypted full phone and hashed identity. Account delete now calls TDLib disconnect/logout hook and soft-deletes the app user's session without deleting shared history/materials.
- Materials/guides can now be soft-deleted via API and UI; lists exclude `knowledge_items.deleted_at IS NOT NULL`, detail returns not found for deleted rows, and source messages/traces remain in DB.
- `/materials` and `/materials/:id` have been restyled and deployed frontend-only to match the current site language: beige/cream background, rounded cards, subtle borders/shadows, account-style header, pale-blue tabs, full-width material cards, and a clean two-column material detail layout. `frontend npm run build` passes; production frontend health OK.
- Backend fix for short-context material misses is implemented, tested, and deployed: `AutoPipelineService` now honors bounded debounce instead of capping to 5s, and `ReplayV2Service` can promote VPN/region-access multi-message discussion segments to `DISCUSSION_SEGMENT_MATERIAL_CANDIDATE` while keeping casual VPN chatter rejected. Full `backend/2.0 mvn test` passes 48/48.
- Old/current material cleanup is prepared as a read-only recommendation list, not applied to production: `reports/materials-cleanup-candidates-20260625.md/.json` recommends `KEEP=8`, `REVIEW=3`, `ARCHIVE_CANDIDATE=2` (`#13`, `#9`).
- Read-only current materials usefulness audit completed: production `/api/v1/materials` has 13 DRAFT materials, average backend quality 90.8/100, types `GUIDE=5`, `GENERATION=6`, `ANSWER=1`, `CLUSTER_SUMMARY=1`; repeated UTF-8 detail audit opened all 13. Report files: `reports/materials-usefulness-audit-20260625.md` and `.json` plus raw API exports.
- `/materials` page has been redesigned locally (not deployed yet) as a reader-friendly library: human labels (`Гайды`, `Заметки`, `Ответы`, `Сводки`), “Начать отсюда” shelf, simple library stats, search/filter panel, CTA-style cards, reading time, source count, and plain-language review warnings. Frontend `npm run build` passes.
- Processing rule changed and deployed backend-only: enabled active dialogs are now processable by default; explicit `auto_pipeline_settings` is no longer required as a processing gate and is used only as config override. `Потребители` is now `ENABLED_PROCESSABLE` in production and latest rows are passing queue/batch/run instead of `AUTO_PIPELINE_DISABLED`.
- P0 pipeline coverage gap diagnosis is complete read-only: `reports/p0-coverage-gap-analysis-20260625.md` explains enabled raw path breaks by reason, top-chat coverage, required raw drilldowns, and fresh live evidence. No mass requeue/reprocess/scoring/proxy/session changes were made.
- Main P0 finding: current fresh live path works for processable chats after SOCKS5 restore; the big gap is historical/backlog plus display-enabled-but-not-processable scopes. `INTAKE_PENDING` splits mainly into `intake_pending_not_queued` and `suppressed_by_scope_no_explicit_auto_setting`.
- Completed a read-only enabled-chat usefulness audit across production `raw_messages`: exported 3354 enabled raw rows and generated `reports/enabled-message-usefulness-audit-20260625.md`, `.csv`, and `.json`; no requeue/reprocess/material generation/threshold changes were made.
- Audit estimate: 424/3354 enabled messages look potentially useful, 107 look guide-ready, 173 need discussion/context, 10 messages are materialized, and 416 useful candidates are not materialized; major miss causes are `PIPELINE_GAP` and single-message false negatives.
- Telegram live ingest is restored: TDLib proxy switched from failed external HTTP `95.164.93.173:3128` to internal SOCKS5 `ssh-socks:9050`, backend was force-recreated only, auth stayed `AuthorizationStateReady`, proxy ping is `OK`, and `raw_messages` advanced to 25 Jun realtime rows.
- `/groups` frontend stability hotfixes are deployed frontend-only: group/message queries keep previous data during refetch, chat list order is pinned, rows are memoized, messages no longer smooth-scroll on every polling refetch, manual chat clicks no longer bounce back, and the `Автообработка` toggle block is removed.
- P0 groups selection regression is resolved: manual clicks rewrite URL to canonical `?group=...`, deep links by `?group=` and `?chatId=` still work, and production browser verified Vibe Dev/Vibemode switching.
- Chat list counters are deployed: `/api/v1/groups` exposes DB/cache-derived `rawMessagesTotal`, `rawMessagesLast24h`, `latestRawMessageAt`, `topicsCount`, `autoPipelineEnabled`, `activeDialog`, `displayState`, `processingState`, and `source`; frontend no longer renders misleading `0/д`.
- Telegram sync/backfill incident fixed: `backfill-from-raw` no longer uses an untyped null topic SQL parameter, production dry-run returns `200`, and TDLib auth state is `AuthorizationStateReady` / `CONNECTED`.
- Material detail correctness is deployed: `/materials/10` shows quality 96/100, real source text, internal app link, honest private-chat Telegram reason, provider calls, and visible message path/trace; no review/action buttons are shown.
- Materials counter audit is complete: removed stale `Полезное` filter and replaced it with real backend artifact filters `Generation` and `Answer`; production `/materials` shows counts `Все=10`, `Гайды=3`, `Generation=6`, `Answer=1`.
- Material page MVP is deployed in production: `/materials` defaults to all material types, shows DRAFT material `10`, card is clickable, and inactive publish/reject/edit/open/delete card actions are removed.
- P0 public-discovery guard is deployed in production: auto pipeline now requires `active TDLib dialog` plus explicit chat/topic auto setting; public discovery/search intake is rejected.
- Production enabled auto settings are reduced to 11 active-dialog explicit settings; suspicious non-active rows `12,13,14,15,16,17` are disabled with `SECURITY_DISABLED_PUBLIC_DISCOVERY` audit reason.
- Production uses clean DB `neuroinfogrinder2_prod_clean`; old DB, TDLib session/auth, Telegram logout, and secrets were not touched.
- Latest backend-only deploy recreated only `backend`; `frontend`, `postgres`, `redis`, `tor`, `ssh-socks`, and `model-worker` remained up/healthy.

Recently done:
- Created `reports/materials-and-signals-20260629.md` plus JSON snapshot with all current production materials/signals from public API: 18 DRAFT materials, 23 signals, 10 topics, full material bodies, sources, how-built steps, provider calls, trace stages, and signal source info.
- Fixed and deployed classifier regression where `Как проверить OpenAI-compatible API...` became `ANSWER`; future equivalent messages should become `GUIDE`. Existing material `92` is still `ANSWER` until explicitly cleaned/reprocessed/edited.
- Fixed the Signals tab topic cards so they no longer show material-count badges/copy; deployed frontend-only and verified health.
- Removed final NIGTOP test outputs after UI review request: materials `87-91` soft-deleted, signals `44/45` deleted; verified active materials/signals/topic links are all `0`.
- Fixed and deployed TDLib live update auto-start/keepalive so live ingest resumes after backend restart without opening the site. Added `TdlibLiveUpdateSupervisor`, gateway, and regression test; backend `mvn test` passed `128/128`; backend-only deploy healthy; production diagnostics show TDLib alive, proxy OK, fresh raw messages, and live-auto runs after restart.
- Ran final `NIGTOP-S01..S10` controlled production topic/material/signal check after cleanup; final run `8420` passed with 5 materials, 2 signals, duplicate suppressed, and topic links populated. Report: `reports/nigtop-controlled-topic-check-20260629.*`.
- Deployed backend fixes discovered by the check: material-topic assignment, summary type preservation, safer reference/risk routing, benchmark service restore, and duplicate suppression hardening. Targeted tests and full backend `mvn -q test` passed.
- Deleted all active materials/guides and all signals before the controlled rerun; production health stayed OK.
- Deployed artifact-type gate fix and validated production run `8258`: correct `GUIDE`, `GENERATION`, `ANSWER`, `SUMMARY`, `REFERENCE`; duplicate excluded from material sources. Removed old test materials `37/38`, deleted temporary dataset `8239`, and wrote `reports/artifact-type-gate-check-20260629.*`.
- Fixed and deployed the NIGTEST lightweight material gate. Controlled rerun `8149` created DRAFT material `37` (`GUIDE`, fallback/API checklist from `A02,A06,A07,A08`) and DRAFT material `38` (`REFERENCE`, Playwright resource from `A03,A04`); run ended `error=null`.
- Restored `Сигналы` as a material filter that opens the knowledge-map mode, with real signal count (`27` in production check) and no duplicate topic grid.
- Deployed the classical ML shadow layer to production backend + model-worker only after full local tests and golden batch. Post-deploy health/status OK; worker inference smoke returns 10-stage output with conservative abstain. Infra services were not restarted; no DB/materialization/settings changes.
- Added local classical ML backend expansion and golden-batch validation: Naive Bayes, calibrated Linear SVM, Decision Tree, Logistic Regression, and Random Forest ensemble support in worker training/inference; uncertainty/modelAgreement output; offline golden runner; `GB01-GB08` stage classification `8/8` with conservative abstain. Reports saved under `reports/classical-ml-golden-batch-20260628.*`. No deploy.
- Fixed production pipeline UI after review: real clickable/styled conveyor filters, no duplicate refresh, stage-message loading through live stage endpoint, stage alias mapping to backend stage IDs, `Потеря` wording/delta from previous stage, and restyled stage filters with non-empty fallback labels. Verified frontend build, frontend deploy, production health, and non-zero stage API counts.
- Converted `/pipeline` and `/pipeline/stages/:stageId` from mockup/static behavior to real read-only pipeline data with live refresh. Added frontend stage-message hook, working filters, live connection status, SSE invalidation for live pipeline queries, backend `status=all` support for stage messages, and explicit `@Autowired` on `ReplayV2Service` constructor; deployed backend+frontend and verified production health/API.
- Added local full-stage classical ML trace/gate coverage: model-worker inference emits preprocessing/dedupe/final Judge gate outputs in addition to existing semantic stages; `DefaultRouteIntelligenceEngine` consumes those outputs to suppress risk, duplicate, and final-gate-rejected candidates. Red tests failed first, then targeted backend regression tests passed. No deploy.
- Deployed topics/signals MVP to production: `backend` and `frontend` recreated/healthy, `V28` applied, topics API works with 10 seeded topics, topic detail works, public URL 200. No backfill/reprocess/materialization. Noted non-fatal classical-ML shadow 404 warnings because current backend worktree expects model-worker endpoints that the untouched production model-worker does not expose.
- Updated `AGENTS.md` with current handoff, active pipeline mockup focus, deployment commands, local run notes, and the partial drawer warning so future agents know exactly where to resume.
- Tightened `/pipeline/messages-mockup` composition again: hint moved above graph, thin integrated ribbon above compact stage cards, loss badges aligned between stages, dotted vertical guide lines down into the stage transition area, and frontend-only deploy completed for production viewing.
- Deployed frontend-only follow-up for TASK 12-15: added the three new pipeline mockup tabs to the sidebar menu, separated Materials/Prompts icons, removed the yellow header `Добавить провайдера` button, kept the add-provider grid card, and verified production health/public URL.
- Created TASK 11 `KNOWLEDGE_GENERATION` prompt preview/report artifacts without changing production prompt or generating materials.
- Added explanation/diagnostic UI improvements to settings, AI providers, cluster detail mockup, and message detail mockup; verified frontend build before deploy.
- Added the high-fidelity `Кластер #1287` detail mockup route with metadata strip, source messages, cluster trace, merge rationale, decision, links, and next step; verified frontend build. No deploy.
- Added the high-fidelity `Карта кластеров` overview mockup route with bubble map, legend, selected cluster side panel, cluster metrics, filters, and latest clusters table; verified frontend build. No deploy.
- Added the high-fidelity `raw_id 4743` pipeline message detail mockup route with source metadata, state/score cards, full message timeline, and expanded LLM Judge rejection details; verified frontend build.
- Added the high-fidelity full `Конвейер сообщений` funnel mockup route with filters, summary metrics, colored narrowing stage ribbon, selected LLM Judge stage cards, message table, rejection chips, and pagination; verified frontend build. No deploy.
- Added the high-fidelity `Embeddings / BGE-M3` pipeline stage detail mockup route with current site styling and verified frontend build.
- Completed the final controlled DISCUSSION_SEGMENT production 24h report after the 24h gate elapsed: refreshed `reports/discussion-segment-controlled-production-24h-20260627.md/.json` from INTERIM to FINAL, confirmed no new DISCUSSION materials after enable and no published materials, active discussion materials remain `23/24/25`, and production health/public URL are clean.
- Ran controlled LLM Judge dry-run for selected MessageUsefulnessClassifier candidates only: raw `11142` and `11484` accepted as reference candidates, raw `11193` did not reach LLM, four other positives hit provider HTTP `502`; no generation calls/materials/settings/caps/threshold/provider-config/Telegram changes; public URL/backend logs clean.
- Implemented and deployed backend-only MessageUsefulnessClassifier inside existing `ReplayV2Service.singleMessageDetection`: red tests failed before class existed, targeted classifier tests passed `10/10`, full backend passed `80/80`, dry-run classified raw `11142` as `NEWS_UPDATE`, raw `11193` as `LINK_ONLY/NEEDS_LINK_ENRICHMENT`, raw `11484` as `RESOURCE_REFERENCE`, and 20 random recent rows as low-value/noise with no false positive candidates. Production backend/frontend health and public URL are clean.
- Fixed and deployed `/materials` count consistency: added backend `OTHER` normalization/counts/filtering, frontend Russian tab labels and conditional `Другое`, regression tests, production API verification, status checks/log check, and required reports. No material rows were mutated.
- Reconciled UI-visible `/groups` messages for `API SUPPORT | ModelHub` to pipeline/raw ids: proved UI API ids are raw ids, mapped GPT-5.6 raw `11142` and lolz raw `11193`, explained previous `0/12` as narrow-window mismatch plus caption source, and generated recovery MD/JSON/CSV reports without changing production state.
- Completed read-only M01-M12/G01-G04 overnight audit and interim controlled-production 24h report. Key finding: expected content did not occur in the specified MSK window; broader-day candidates exist for some items (for example OpenMontage raw `11484`), but exact source ids are needed before manual materialization. Post-check confirmed settings unchanged and active DISCUSSION materials still `3`.
- Fixed generated material writing style and source transparency: added red/green prompt/API tests, backend API fields for ordered multi-source detail/how-built/provider previews, frontend multi-source detail UI, deployed backend+frontend, verified materials `23/24/25` read-only, and generated required reports. No backlog/reprocess/new material generation/body rewrite/provider config/threshold/Telegram/public-discovery changes.
- Enabled DISCUSSION_SEGMENT controlled production mode after adding caveat/risk/cap/fresh-only gates, deployed backend-only, set production settings to CONTROLLED, captured effective and monitor snapshots, verified services/logs/public URL, and wrote enable plus initial 24h reports. No backlog/reprocess was run and no post-enable materials were created yet.
- Added controlled-mode backend guardrails and regression coverage: DRAFT-only, fresh-only, daily cap, per chat/topic cap, LLM-accepted required, risk-sensitive skip, duplicate guard, provider/generation error stop, and provider-doc caveat for cache/pricing/API material prompts. Full backend tests passed 62/62.
- Ran the first approved controlled DISCUSSION_SEGMENT materialization for S0024/S0269/S0132 only. Created DRAFT materials 23/24/25, verified list/detail/source/trace/provider API and public UI routes, generated required reports, and confirmed production health/logs.
- Completed manual accepted-judge review: inspected source messages for S0024/S0269/S0132/S0217/S0021/S0266, checked LLM title/reason/outline grounding, type correctness, E07 safety, and duplicate/merge relations; created review MD/JSON/CSV and confirmed no generation/materialization.
- Ran tightened LLM judge dry-run on 10 selected accepted segments only, excluding E08/E09/E10 and all rejected/duplicate/window-limit candidates; saved provider diagnostics and parsed decisions, reported four provider 502 errors explicitly, and confirmed no discussion generation/materials.
- Tightened DISCUSSION_SEGMENT scorer/dedupe/type calibration backend-only: added regression coverage for overlap merge, window cap, entity-only rejection, risk/context rejection, and production-like E04 SUMMARY; verified red/green path, full `mvn test` `60/60`, deployed backend-only, regenerated comparison reports, and confirmed generation/material count remained zero.
- Ran selected LLM judge dry-run only for five approved discussion segments and persisted provider call diagnostics/parsed decisions; did not call knowledge generation or create discussion materials. Noted unrelated live-auto material creation during the same period as an acceptance caveat.
- Deployed backend-only DISCUSSION_SEGMENT MVP with `V24`, kept generation disabled, verified production health/public URL/service uptime, ran bounded dry-run diagnostics for the overnight window, persisted only diagnostic segment/source rows, and confirmed no provider calls or knowledge items/materials were created. Backend logs after dry-run had no errors.
- Completed manual ground-truth audit over all 919 overnight rows: generated `manual-classes`, `manual-clusters`, `manual-expected-materials`, `manual-vs-pipeline-gaps`, `manual-discussion-segments`, `manual-single-message-false-negatives`, and `manual-existing-materials-review` CSVs plus Markdown/JSON report; no reprocess/backlog/material generation/config changes were made.
- Completed P0 overnight audit and safe backend bugfix/deploy: verified full raw->material funnel for all 919 overnight processable rows, identified material generation failure for raw `6861`, added regression test, fixed judge decision whitelist, ran `mvn test` 49/49, deployed backend-only, and confirmed production health/logs.
- Implemented and deployed the account/session/material deletion request: secure phone helper using AES-GCM/HMAC when env keys exist, identity/session schema, account create/list soft-delete changes, TDLib `disconnectAndForget`, material soft-delete endpoint, account disconnect UI copy, and delete controls in material list/detail. Verified backend tests 48/48, frontend build, production health, accounts/materials API smoke, and clean backend logs.
- Completed UI task for materials pages: `/materials` now uses account-style header/filter/cards with no legacy `Полезное 0` or action buttons; `/materials/:id` now uses a dedicated material detail view with article card, source message, quality, provider, why-created, trace timeline, and compact diagnostics instead of raw/debug tabs. Deployed frontend-only and verified production status/API details.
- Implemented and verified backend regression fixes for the VPN/WARP/OpenVPN miss: added red/green tests for debounce and discussion-segment detection, fixed 5-second debounce cap, added segment heuristic for region-access VPN discussions, and ran full backend tests 48/48.
- Prepared safe material cleanup candidate reports under `reports/`; no production material delete/archive was executed.
- Audited current production materials read-only and found the set is small but mostly useful technical/dev/AI ops drafts; the main problem is UX/trust/triage rather than total junk content. Added local report artifacts under `reports/`.
- Reworked `MaterialsPage` locally twice after UX feedback: replaced operator-style metrics with a reader-oriented library UI, clearer labels, recommended shelf, CTA cards, source/reading-time cues, and plain warnings; added `CLUSTER_SUMMARY` to frontend content types; verified `cd frontend && npm run build`.
- Implemented default processing for display-enabled active chats in backend `PipelineLiveService`, `AutoPipelineService`, and group DTO SQL; added regression coverage, verified `mvn test` 45/45, deployed backend-only, and production-verified `Потребители` as processable with latest rows processed through queue/batch/run.
- Diagnosed P0 coverage gap with production exports: fresh 80-row post-SOCKS5 sample had `intake/queue/batch/run=PROCESSED/COMPLETED` for 80/80 and single-message decisions for 80/80, while backlog showed `995` display-only scope suppressions, `993` intake-not-queued rows, `642` replay-without-embedding rows, and `399` run-without-replay-message rows.
- Ran enabled-message usefulness audit and produced full report files under `reports/`; the report is heuristic triage, not a final human-labeled ground truth, and it intentionally includes broad usefulness beyond dev/API/AI.
- Fixed P0 display-layer regression in `GroupsChatView`: preselected chat changes were ignored after an initial selection, so navigation/link checks could remain on the old chat or empty pane. Added support for selecting by `group` or `chatId` query param without weakening P0 guard.
- Fixed chat-list UI counters: `0/д` came from `GroupListPanel` rendering `Number(group.messagesPerDay)/д`; `/д` meant messages/day but was a legacy last-24h counter. UI now shows `N за 24ч`, `Всего N`, `0 сообщений`, or `—` without null-to-zero fallback for new counters.
- Extended backend group DTO with explicit display vs processing state. `on` badge now maps to `processingState=ENABLED_PROCESSABLE`, not “has messages”; counters come from DB history, not TDLib live status.
- Diagnosed Telegram sync complaint: services were healthy and live updates/backfill were persisting messages, but `/api/v2/pipeline/intake/backfill-from-raw` failed with PostgreSQL `could not determine data type of parameter $3` when `topicId=null`; added regression test and deployed backend-only fix.
- Verified production after deploy with dry-run backfill response `rawFound=5`, `skippedExisting=5`, `queued=0`, `dryRun=true`; no new SQL 500s in backend logs.
- Fixed material detail API null-score and source mapping: `confidence` remains null instead of defaulting to 0, source text falls back through raw text/caption, dataset text/caption, and raw JSON, and private Telegram chats no longer get invented `t.me` links.
- Added material detail `providerCalls`, `traceStages`, `run`, `candidate`, `rawId`, `appMessageUrl`, text unavailable reasons, and Telegram unavailable reasons.
- Updated material detail UI to be read-only for materials, display `Качество` instead of false `Полезность 0/100`, render source text/link/reason, show provider calls and a visible `Путь сообщения` trace block.
- Audited overnight materials and missed messages read-only. No scoring/backlog/requeue changes were made. Main finding: current single-message path misses near-threshold API/error troubleshooting fragments; discussion segment is likely needed before threshold changes.
- Extended frontend `ContentType` handling for `GENERATION` and `ANSWER`; stale `Полезное` used legacy `USEFUL_INFO/...` types that production `knowledge_items` does not write.
- Fixed `/api/v1/materials` all-types query so it works without `contentType`; production API returns material `10` with `contentType=GUIDE`, `status=DRAFT`, `publicationKind=SINGLE_MESSAGE`.
- Simplified `MaterialsPage` card MVP: whole card opens detail, default type tab is `Все`, `Гайды` is only a filter, and disabled/non-working review buttons are gone.
- Verified `npm run build`, backend `mvn test` 42/42, frontend and backend deploy health, and browser click-through from `/materials` to `/materials/10`.
- Audited code for dangerous TDLib methods: no app-code `SearchPublicChats`, `SearchChatsOnServer`, `JoinChat`, invite import, or add-member calls; only `GetChatHistory` existed and is now guarded by active dialog.
- Found root source for public/outside-scope processing risk: legacy/global `auto_pipeline_settings` matching plus queue/backfill/run paths could process raw cached chats without active dialog proof.
- Added `V22__auto_pipeline_security_guard.sql` audit columns and marked legacy settings source; disabled suspicious production settings `12,13,14,15,16,17` without deleting rows.

Next:
- If TASK 11 prompt preview is approved, apply it as a controlled production prompt update only after a prompt-test path is safe for non-null run/provider-call recording; do not mutate production prompt from preview alone.
- If the new pipeline mockup menu entries are accepted, decide which mockups should become real live pipeline pages and connect them to backend APIs instead of static data.
- If the cluster map mockup is approved, next implementation step would connect bubbles/table/selected cluster panel to real clustering APIs and add route navigation from the pipeline funnel.
- If the full funnel mockup is approved, decide whether it should replace/enhance the current live `/pipeline` route or remain a design reference; implementation follow-up would connect stages/table to real pipeline APIs instead of static data.
- If Adam explicitly approves controlled materialization, start only with raw `11142` and/or raw `11484` as DRAFT REFERENCE/SUMMARY candidates with caveats; do not materialize raw `11193`, and do not materialize HTTP-502 inconclusive candidates without a separate judge-only retry.
- If Adam wants data cleanup beyond UI visibility, review whether legacy `cluster_summary` should be migrated to `SUMMARY` and whether `REFERENCE` should become first-class instead of `OTHER`; do not mass-update without approval.
- Consider adding a UI-to-pipeline resolver endpoint/report for `/groups` message ids and explicit link-only/news-reference rejection modes; raw `11142` is a candidate for manual SUMMARY/REFERENCE only if Adam approves, while raw `11193` should remain rejected unless link enrichment is added.
- If Adam wants manual materials from the overnight expectation list, first confirm exact source raw ids because M01-M12 did not match the specified `03:40-04:15 MSK` window. Clearest broader-day candidate is G03/OpenMontage raw `11484`; G01/G02 need exact source selection; G04 must remain safety-summary-only or reject.
- If Adam approves fixing existing content, regenerate or manually edit only materials `23`, `24`, and `25` to remove old meta-chat phrasing; do not do this automatically.
- Monitor stop conditions: more than 3 DISCUSSION_SEGMENT materials in one UTC day, any auto-created PUBLISHED material, rejected/needs-context/risk-sensitive materialization, missing source/trace, duplicate spam, repeated provider errors, hallucinated material, backend runtime errors, or public discovery regression.
- Review material `24` wording before any broader rollout; future generations include the caveat, but the existing material was not rewritten without approval.
- If continuing controlled materialization manually, next candidate could be S0217/E07 only with explicit approval and strict no-medical-claims wording; do not switch to broad/unlimited DISCUSSION_SEGMENT generation.
- Do not approve broad generation. If Adam explicitly approves controlled materialization, use max 4 candidates: S0024 merged with S0021, S0269 merged with S0266, S0132, and S0217. Do not materialize S0021 or S0266 separately.
- Do not retry HTTP-502 segments automatically; if needed, run a separate judge-only retry for S0222/S0135/S0109/S0112.
- Keep monitoring for unintended discussion over-generation in live runs; backend now persists explicit rejection reasons for overlap/window-limit/low-value/entity-only candidates when discussion detection runs.
- If continuing from manual audit, prioritize a design-only `DISCUSSION_SEGMENT` plan using fixtures E01/E02/E04/E07 from the report; do not implement or reprocess without explicit approval.
- Add targeted scoring fixtures for raw `6224`, `6536`, `6765`, and `6105` before any threshold change; recommendation is targeted signal boosts, not lowering the global `0.55` threshold.
- If Adam wants to materialize the missed positive candidate, run only a controlled single raw-id reprocess for raw `6861` after explicit approval; do not mass requeue/backlog.
- Separately consider trace persistence fix: live `replay_run_messages.raw_message_id` is currently `NULL`; reports must join via `dataset_messages`, or backend should populate raw id in a safe follow-up.
- To fully validate full-phone display, add or reconnect a Telegram account after V23 with phone privacy keys present; legacy rows cannot recover full phones from masked values.
- Shared identity read-path is partially prepared through `identity_id` on new rows; complete cross-user read dedupe should be validated once real app users replace the current hardcoded `userId=1` auth stub.
- If further UI acceptance is needed, use an authenticated browser session to capture screenshots for `/materials`, `/materials/10`, and `/materials/11`; WebBridge new-tab checks currently redirect to `/login` without user session, while API checks for material 10/11 pass.
- Verify the deployed backend fix on a fresh multi-message VPN/region discussion or a bounded approved replay sample; do not mass requeue old backlog automatically.
- If Adam approves cleanup, apply only the exact selected material IDs from `reports/materials-cleanup-candidates-20260625.md`; current recommendation is archive candidates `13` and `9`, review `14`, `12`, `10`, keep the rest.
- If Adam approves backlog handling, run only a bounded dry-run plan for processable chats (`enabled + activeDialog`), with dedupe/source-hash checks and DRAFT-only material policy; do not mass requeue.
- Continue monitoring `/api/v2/telegram/diagnostics`: expected proxy state is `SOCKS5`, port `9050`, `proxyPingStatus=OK`, auth `AuthorizationStateReady`.
- If continuing, implement review flow only when explicitly requested; current material MVP intentionally does not publish/reject/edit.
- Do not run backlog/requeue until public-discovery guard remains stable and Adam explicitly approves.

Open questions:
- Final unique-text acceptance for a manually sent `live-ingest-socks5-*` Pasha message still needs to be run if Adam wants exact DB/API/UI evidence for that specific message; general realtime ingest is already confirmed by new 25 Jun raw rows.
- Whether duplicate enabled settings for chat `-1002922797592` (`setting_id=1` topic scope and `10` chat scope) should be deduplicated; both are active dialogs, so left unchanged.
- Whether to backfill/reprocess old fake `NO_CLUSTERS` live-auto runs; requires explicit operator approval.
